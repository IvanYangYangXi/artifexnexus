# 2026-05-28 工作日志

## GitHub 推送（SkillHub + Notification）

`main` → `origin/main`，两次提交：

1. **`314cc9d`** `feat(dcc): SkillHub 全平台基建 + @skill_tool 装饰器统一`
   - 38 files, +2598/-454
   - 共享 SDK: skill_hub.py, skill_manifest.py, decorator.py
   - Blender/Maya/Max 全部接入 SkillHub
   - @skill_tool 统一 + @ue_tool 移除
   - QA: P0 修复 3 个, P1 修复 4 个

2. **`13f98fd`** `feat(notification): 通知系统 Tauri IPC + OpenClaw cron 双通道 + 工具详情工具提示`
   - 7 files, +423/-154

`.workbuddy/` 已排除（不提交）

## SkillList 发布/更新按钮（对齐 ArtClaw 双按钮模型）

### 需求
- 所有已安装 skill 显示发布按钮
- 加载列表时获取版本管理信息（skillCheckSync）
- needs_publish / needs_update → 高亮按钮；正常 → 常规按钮

### 修改（仅 SkillList.tsx）
- 导入 `skillCheckSync` + `SyncStateInfo`
- 新增 `syncStates` 状态（Record<string, SyncStateInfo>）
- `loadSkills` 用 `Promise.allSettled` 批量查同步状态
- 替换旧"同步+仅用户层发布"为双按钮：
  - `needs_update` → 高亮"更新" (variant=default, skillSync)
  - `needs_publish` → 高亮"发布" (variant=default, 弹窗/publish)
  - 无 pending → 常规"发布" (variant=outline, 用户层紫色)
- TS 编译: 0 errors
- 计划文档: docs/specs/skill-publish-button-v2.md

### 发布按钮 bugfix（2026-05-28）
- 问题: doPublish 对非用户层 skill 走 doAction→skillPublish 直接发，不弹窗
- 修复: doPublish 统一 setPublishTarget 弹窗；onConfirmSkill 的 source_layer 从 "02_user" 改为 publishTarget.layer

### PublishConfirmDialog 修复（2026-05-28）
- 问题1: SKILL_TARGET_LAYERS 错误包含 "01_team"（不存在的层级）
- 修复1: 改为 "01_marketplace"（市集）+ "00_official"（官方）
- 问题2: useEffect 每次 skillData 引用变化就重置 skillTargetLayer 为默认值，导致选官方后跳回市集
- 修复2: 用 prevOpen ref 追踪 open，仅在 false→true 时重置状态
- TS 编译: 0 errors

### PublishConfirmDialog 版本编辑 + 默认层级（2026-05-28）
- 需求: 版本可编辑 + 校验格式(x.y.z)和大于当前版本 + 默认层级按源层级适配
- 新增 skillVersion 状态 + Input 替代只读文本
- validateSkill: 空值 → 格式 x.y.z → > 当前版本（_isVersionGt 逐段比较）
- 默认目标层级: 02_user → 01_marketplace，否则 → 源层级（useMemo）
- SkillPublishData 加 layer / SkillPublishResult 加 version
- skill-api: skillPublish opts 加 version?
- SkillList: skillData 传 layer, onConfirmSkill 传 result.version
- TS 编译: 0 errors

### 发布后仍显示 needs_publish 修复（2026-05-28）
- 根因: installer.publish() 不接受 version 参数 + skill_rpc 不调 _invalidate_scan_cache()
- Fix 1 (installer.py): publish() 新增 version 参数 → 写入 manifest.json → 返回 resolved_version
- Fix 2 (skill_rpc.py): _handle_skill_publish 提取 params.version 传给 installer + 成功后调 _invalidate_scan_cache()
- Python 编译: OK, TS: 0 errors

### publish 后仍 shows needs_publish 深度修复（2026-05-28）

**根因分析**:
installer.publish() 只更新 flat install dir (`~/.artifexnexus/.openclaw/workspace/skills/<name>/manifest.json`)
但 _skill_check_sync 比较 install_dir 和 hub entry.path（project source dir，如 `skills/official/<name>/`）。
当 skill 同时存在于两处时，project source dir 的 manifest.json 未更新 → compare_skill_dirs 检测到差异 → needs_publish 恒为 true。

**Fix 1 — installer.py publish()**:
- 写入 install dir manifest.json 后，同步更新 target_layer 源码目录的 manifest.json
- 通过 `self._layer_sources` 查找 target_layer 对应目录
- 用 `_source_skill_dir` 定位 skill，存在则更新其 manifest.json
- 所有异常均记录 warning 日志，不阻断成功返回
- 改进原有 silent fail（except pass）为 logger.warning

**Fix 2 — SkillDetailPanel.tsx 版本号不可编辑**:
- 移除 `<input>` 编辑框，改为 `<p>` 只读显示
- 保留 version state + versionValid（供保存逻辑用，始终为 true）
- TS 编译: 0 errors

### Staging 代码不同步导致所有修复未生效（2026-05-28）

**关键发现**: Sidecar 运行时从 `apps/desktop/staging/` 加载 Python 代码和 skills 数据。
而我们所有修改都在 `packages/` 源码目录。Staging 目录有 34 个文件与源码不一致。
文件权限正常（W_OK=True），非只读问题。

**同步到 staging 的文件**:
- installer.py, skill_rpc.py, _rpc_helpers.py, hub/core.py, registry.py
- decorator/core.py, categories.py, nexus_tool/installer.py, nexus_tool/scanner.py
- `skills/official/` 和 `skills/marketplace/` 完整目录

**⚠ 需要重启 Gateway/Sidecar 使新代码生效！**

### publish/sync 重构为暴力全量覆盖（2026-05-28）

**用户洞察**：之前的增量同步（只改 manifest.json）导致源码目录其他文件不更新，publish 后两端不一致。

**新策略 — publish()**（安装目录 → 源码目录）：
1. 更新 install_dir/manifest.json 版本号
2. `shutil.rmtree(source_dir)` 删除旧源码目录
3. `shutil.copytree(install_dir, source_dir)` 全量复制
4. 代码约 55 行（原 100 行），去掉复杂的增量逻辑

**新策略 — _do_sync_copy()**（源码目录 → 安装目录）：
1. `shutil.rmtree(target_dir)` 删除旧安装目录
2. `shutil.copytree(source_dir, target_dir)` 全量覆盖
3. 代码约 20 行（原 35 行）

**效果**：两端文件完全一致，`compare_skill_dirs` 返回 `SYNCED`。

**文件**：
- `packages/platform/skill/src/artifex_nexus/skill/installer.py` — publish() 重写 + _do_sync_copy() 重写
- 新增 `_read_skill_version()` 辅助方法
- Staging 已同步
- ⚠️ 需要重启 Gateway/Sidecar

## 全量文档整理与三大新目标（2026-05-28 第二轮）

### 文档整理（4 Batch + QA 审核）
审计范围：docs/ 92 文件 + .ai/ 11 文件 = 103 文件，发现 57 个问题（P0x11, P1x22, P2x24）

**Batch 1 - 路径修复（P0）**：
- EPIC-0003~0006 backlog→done，STORY-0050 in-progress→done
- nexus-tool-trigger-persistence.md → decisions/0010-trigger-persistence.md (ADR 0010)
- stories/ + _handoff/ + in-progress/ 目录删除
- 删 1.8MB+366KB JSON 膨胀文件
- extract-schema.mjs → scripts/

**Batch 2 - frontmatter 修复（P1）**：
- 7 个文件补 YAML frontmatter
- board.md Kanban 同步更新

**Batch 3 - 过时内容修复（P1）**：
- SDK docs junction/symlink→copy（ADR 0008）
- Skill 模板废弃装饰器 → @skill_tool
- inbox 清理：中文文件名→installer-wizard.md
- 创建 non-goals.md + guides/onboarding.md
- 7 个 spec status draft→accepted

**Batch 4 - .ai/ 指引文档修复（P0/P1）**：
- UEClawBridge→ArtifexNexus
- 扩展 DCC 列表（+ Maya/Max/ComfyUI/Houdini/SP/SD/Unity）
- @skill_tool 标明为主，@artclaw_tool 降级为历史别名
- Blender 5.1 内部约定注释
- MCP 命名 + 主线程安全警告扩展至 4 个 DCC
- Vibe Coding→Agentic Development

### QA 机制
每批完成后即 spawn QA agent 审核，3 批共发现 6 处 FAIL（均即时修复），最终全部通过。

### 三大新目标 → inbox
1. `docs/inbox/platform-extension-api.md` — 平台化 DCC 接入 API
2. `docs/inbox/data-visualization-view.md` — 数据图形视图+反向编辑
3. `docs/inbox/workflow-node-editor.md` — 节点式工作流编辑器

## 全量文档审计与更新（2026-05-28 第一轮）

### 背景
项目功能与开发进度已远超文档描述。M3-M6 实际已完成，但路线图/EPIC 仍标记为 backlog。
执行全量双边对照：文档 vs 代码功能 → 更新所有文档。

### 核心偏差发现
| 里程碑 | 文档状态 | 实际代码 | 偏差 |
|--------|---------|---------|------|
| M3 Web UI+Chat | backlog | 完整四面板 App Shell + Chat/Skills/Tools/Calendar | 🔴 严重 |
| M4 Skill系统 | 未创建EPIC | 完整 Hub/Registry/Installer + 66个Skills | 🔴 严重 |
| M5 Tool系统 | 未创建EPIC | 完整 Registry/Installer + 8个Tools | 🔴 严重 |
| M6 记忆 | backlog | 三层记忆体系完整 | 🟡 中等 |
| M7 多DCC | 未创建 | Maya/Max插件骨架+Gateway Bridge+前端UI已存在 | 🔴 严重 |
| DCC范围 | UE5.7+Blender | 实际 Maya/Max 插件+ComfyUI/SD/SP/Houdini/Unity Skills | 🔴 严重 |

### 更新的文件（17个）

**核心愿景/路线图**：
1. `docs/vision/roadmap.md` — 全量重写，M0-M9 状态标注 ✅/🔄/📋，新增"当前焦点"优先级表
2. `docs/vision/north-star.md` — 修正非目标（Maya/Max不再被排除），新增DCC覆盖表，成功指标表格化

**架构/项目概述**：
3. `.ai/context/project-overview.md` — DCC范围扩展到4+，新增端口表/Web UI功能清单/包索引更新
4. `.ai/context/glossary.md` — 新增 Maya/Max/Shared SDK/Sidecar/Tauri/App Shell/Notifications 术语
5. `docs/specs/系统架构设计.md` — status draft→accepted，新增DCC插件架构章节、端口总表、Shared SDK详解
6. `openspec/config.yaml` — 更新 active_work（替代 active_epic）、roadmap状态、技术栈

**EPIC 状态**：
7. `docs/tasks/backlog/EPIC-0003-m3-web-ui-chat.md` — status done，新增实际交付清单（超出原计划）
8. `docs/tasks/backlog/EPIC-0004-m4-skill-system.md` — status done，详细列出所有模块和66个Skills
9. `docs/tasks/backlog/EPIC-0005-m5-nexus-tool-system.md` — status done，8个Tools+三态触发系统
10. `docs/tasks/backlog/EPIC-0006-m6-memory-system.md` — status done，三层记忆体系详细说明
11. `docs/tasks/backlog/EPIC-0007-m7-multi-dcc-inapp-chat.md` — status in-progress，当前进度概览表
12. `docs/tasks/backlog/EPIC-0008-m8-comfyui-workflow.md` — status planned，标注14个Skills已创建
13. `docs/tasks/backlog/EPIC-0009-m9-extra-dcc.md` — status planned，标注SP/SD/Houdini/Unity Skills已创建
14. `docs/tasks/done/EPIC-0001-m1-onboarding-install.md` — status done→review（审查发现P0问题）

### 未修改但已验证准确的文件
- `.ai/rules/00-architecture.md` — 架构铁律是原则，无需修改
- `.ai/rules/10-coding-style.md` / `20-docs-workflow.md` / `30-agent-behavior.md` / `40-build-and-release.md` — 流程规范，无需修改
- `docs/development/web-frontend-setup.md` — Tailwind v4 + pnpm 配置指南准确
- `docs/decisions/` — 8个ADR内容准确

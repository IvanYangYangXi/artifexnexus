# 2026-05-27

## 对话列表自动清理机制

- 新增 `session-cleanup.ts`：扫描 Gateway sessions + IndexedDB + localStorage，识别过期/空会话并清理
- 清理规则：空会话 >24h + 无 transcript → 自动删除；过期会话 >30d 未活动 → 自动删除
- 修改 `persistence.ts`：新增 `deleteSessionsBatch()` 批量删除 + `cleanLocalStorageCaches()` 清理 localStorage
- 修改 `chat-service.ts`：新增 `cleanExpiredSessions()` 方法清除内存缓存
- 修改 `ChatView.tsx`：在 Gateway 首次运行后触发 `performAutoCleanup()`
- 静默清理：console.log 记录日志，不弹 toast
- TypeScript 编译通过（0 errors）

## 清理时机优化 + 空白对话处理（第二轮）

### 调研结论
- OpenClaw 会话管理：`~/.artifexnexus/.openclaw/state/agents/<agentId>/sessions/sessions.json`（append-only）
- Transcript 文件：`.jsonl` 格式，`sidecar_sessions.py` 的 `_extract_session_summary` 返回 `hasTranscript` 字段
- OpenClaw 本身无自动清理机制，删除会话仅给 transcript 文件名加 `.deleted.` 后缀
- 空白对话原因：`hasTranscript=false` 的会话仍在列表中，点开时 transcript 文件不存在 → 返回空消息

### 优化措施
1. **SessionSummary 类型**（`openclaw.ts`）：新增 `hasTranscript?: boolean` 字段
2. **即时过滤**（`ChatControlBar`）：会话加载后立即隐藏 `hasTranscript=false` 且创建 >24h 的空会话
3. **延迟清理**（`ChatControlBar`）：会话加载成功后延迟 30s 执行 IndexedDB/localStorage 清理，不阻塞 UI
4. **移除竞争**（`ChatView`）：删除 ChatView 中的启动清理 useEffect，消除与 ChatControlBar 的 IPC 竞争
5. **回调通知**：ChatControlBar 通过 `onCleanupComplete` 通知 ChatView 清除 chat-service 内存缓存 + 刷新列表

## 空白对话修复 + IndexedDB 分块清理（第三轮）

### 根因
- **Rust `SessionSummary` 结构体缺少 `has_transcript` 字段**，手动反序列化也未提取，导致 sidecar 返回的 `hasTranscript` 在 Rust 层被静默丢弃
- TypeScript 端的 `hasTranscript` 永远为 `undefined`，`s.hasTranscript !== false` 恒为 `true`，过滤失效

### 修复
1. **Rust**：`openclaw_sessions.rs` 的 `SessionSummary` 新增 `pub has_transcript: Option<bool>`，反序列化中提取 `s["hasTranscript"].as_bool()`
2. **IndexedDB 分块清理**：`deleteSessionsBatch` 重写为分块执行（每 5 条一块，块间 `setTimeout(0)` yield 事件循环），避免长时间阻塞主线程
3. **Rust + TypeScript 双端编译通过**

## 工具详情页底部按钮自适应布局

- 文件：`packages/apps/web/src/components/skills/ToolDetailPanel.tsx`
- 保存修改按钮移至极左，刷新按钮移至极右，另存为实例居中
- 修正：改为统一阈值，所有文字一起显示/隐藏，避免个别按钮被裁切
  - ToolDetailPanel：容器 ≥ 260px 显示全部文字，否则仅图标
  - RunPanel：容器 ≥ 230px 显示全部文字，否则仅图标

## 日历面板功能开发

### 实现文件
- 前端新建 11 个文件：`lib/calendar/types.ts`, `cron-utils.ts`, `api.ts`, `components/calendar/CalendarPage.tsx`, `CalendarHeader.tsx`, `CalendarFilterBar.tsx`, `MonthView.tsx`, `WeekView.tsx`, `DayView.tsx`, `TaskListPanel.tsx`, `TaskItem.tsx`
- 前端修改 3 个文件：`Sidebar.tsx`（calendar 模块）、`ContentArea.tsx`（calendar 路由）、`ipc/openclaw.ts`（readOpenClawCronJobs）
- Rust 新建 1 个文件：`commands/openclaw_cron.rs`（读取 cron jobs），注册到 `mod.rs` + `lib.rs`

### 验证
- tsc --noEmit: 0 errors
- cargo check: passed

### 日历面板第二轮修复（2026-05-27）

**核心Bug：cron/interval 任务只在当天显示**
- 根因：`computeRunTimes` / `buildOpenClawTask` 只对传入的单个 `date` 计算 runTimes，不覆盖当月其他日期
- 修复：改为遍历当月所有 days，逐日计算并聚合 runTimes
- 高频任务（每5分钟）会产生 ~8640 条 runTimes，但 `isHighFreq` 标记已确保显示层面合并展示

**OpenClaw 任务点击无预览**
- 新增 `calendar-task-detail` preview kind
- `RightPanel.tsx` 新增 `TaskDetailPreview` 组件：显示来源标签、启用状态、调度详情、执行时间列表
- `handleTaskClick` 统一处理 nexus-tool → ToolDetailPanel，openclaw → TaskDetailPreview

**周/日视图任务不可点击**
- WeekView：新增 `onTaskClick` prop，`WeekTaskBar` 组件支持点击标题查看详情
- DayView：新增 `onTaskClick` prop，`TaskCard` 添加 onClick + hover 效果
- CalendarPage 传递 `handleTaskClick` 给所有三个视图

**UI 显示不统一**
- WeekView 任务条：统一为 色点 + 标题 + 周期描述 + 启用标签（非纯色背景白字）
- DayView TaskCard：统一为 色点 + 标题 + 周期描述 + 时间列表 + 启用标签
- TaskItem 保持不变（底部面板中已统一）
- 周/日视图任务条添加 hover ring 效果

### 日历面板第三轮修复（2026-05-27）

**1. 底部任务列表 TaskItem 统一**
- 移除 Cron 任务的 `disabled` 属性，所有任务可点击预览
- 统一样式：cursor-pointer + hover:bg-accent/20

**2. 周/日视图日期选择**
- WeekView：时间槽恢复为 `<button onClick={onDateClick}>`，空槽也可点击选日期
- DayView：日期标题改为 `<button onClick={onDateClick}>`，支持点击选择

**3. 高频阈值调整（<= 4h）**
- `isHighFrequency` 从 `< 60min` 改为 `<= 240min`
- 高频任务从日历视图（月/周/日）中完全过滤，仅底部面板显示
- CalendarPage 新增 `viewTasks` memo（filteredTasks 排除高频），供视图使用
- MonthView 移除高频⚡标记，dateTaskCountMap 简化为单个 tasks 数组
- WeekView/DayView 移除高频汇总区
- 筛选标签更新：高频 (≤4h) / 常规 (4h-12h)

**4. 周视图任务条颜色**
- WeekTaskBar 从彩色 fill (`${color}18`) 改为固定灰度 `bg-muted/40`
- 仅保留色点颜色标识，文字不再受底色影响

**5. Cron 任务对话列表预览**
- Rust: `CronJobEntry` 新增 `agent_id` / `session_key`；IPC 类型同步
- `CalendarTask` 新增 `agentId?` / `sessionKey?` 字段
- `TaskDetailPreview`：OpenClaw 任务加载关联 agent 最近 10 条对话
- 每条对话显示标题/日期/token数，点击发送 `artifex:switch-session` 事件切换对话

1. **嵌套 button 错误**：TaskListPanel 头部 `<button>` 内包含关闭 X `<button>`，违反 HTML 规范。改为外层 `<div role="button">`
2. **高频任务不显示工具名**：TaskItem compact 模式下隐藏了 toolName，改为始终显示（compact 模式 `text-[9px]`，normal 模式 `text-[10px] font-medium`）
3. **普通任务工具名不够明显**：从 `text-[9px] text-muted-foreground` 改为 `text-[10px] font-medium text-foreground/60`，并加背景色标记
4. **OpenClaw 任务完全不显示（根因）**：jobs.json 格式是 `{version, jobs: [{schedule: {kind, expr, at, tz}}]}`，但 Rust 结构体按 `[{schedule: string}]` 解析，serde 反序列化失败返回空列表。修复：
   - Rust: 新增 `CronJobsFile` / `CronJobSchedule` 结构体，正确匹配嵌套格式
   - TS IPC: `OpenClawCronSchedule` 类型替换原来的 `schedule: string`
   - api.ts: `buildOpenClawTask` 重写，支持 `kind: cron` 和 `kind: at` 两种 schedule 类型

### 修复 cron 类型任务无法识别为高频任务（2026-05-27）

- **Bug**: "每小时合规检查"用 cron `"0 * * * *"` 配置，但 `isHighFrequency` 只处理 `interval` 类型，`config.type === "cron"` 的任务直接跳过
- **修复**:
  1. `cron-utils.ts`: 新增 `isCronHighFrequency(cron)` — 分析 min/hour 字段的 `*/N` 步进或 `*` 通配，估算最小执行间隔，≤240min 即为高频
  2. `api.ts`: 新增 `isTaskHighFrequency(config)` 统一处理 interval 和 cron 两种类型
  3. `buildNexusTask`: `isHighFreq` 改用 `isTaskHighFrequency(config)`
  4. `buildOpenClawTask`: `isHighFreq` 从硬编码 `false` 改为 `isCronHighFrequency(schedule.expr)`
- tsc --noEmit: 0 errors

### 周视图日期标题可点击 + 高频任务 ⚡ 标记（2026-05-27）

- 周视图日期标题区 `<div>` → `<button onClick={onDateClick}>`，支持点击切换选中日期
- 高频任务恢复日历视图显示：CalendarPage 新增 `highFreqDateSet`（当月有高频任务的日期集合）
  - MonthView: 日期格右上角显示 ⚡
  - WeekView: 日期头部星期文字旁显示 ⚡
  - DayView: 日期标题行显示 "⚡ 高频任务"
- tsc --noEmit: 0 errors

### 修复 Cron 任务关联对话（2026-05-27）

**Bug 1: 对话列表错误** — 加载 agent 最近 10 条对话，未按 cron 任务过滤
- 根因：cron 任务在 sessions.json 中的 key 模式为 `agent:{agentId}:cron:{jobId}`（isolated 运行加 `:run:{uuid}`），原代码直接取前 10 条
- 修复：
  1. `types.ts`: CalendarTask 新增 `jobId?: string` 字段
  2. `api.ts`: buildOpenClawTask 两处设置 `jobId: job.id`
  3. `TaskDetailPreview`: 按 `agent:{agentId}:cron:{jobId}` 前缀过滤 sessions，上限 10 条

**Bug 2: 切换按钮无效**
- 根因：`window.dispatchEvent("artifex:switch-session")` 发出了但 ChatView 无 listener
- 修复：
  1. `ChatView.tsx`: `handleSwitchSession` 改为 `useCallback`，新增 `useEffect` 监听 `artifex:switch-session` 事件
  2. 事件触发 → `handleSwitchSession(sessionKey)` → `setActiveSessionKey` + `chat.switchSession` + `silentLoadHistory`
- tsc --noEmit: 0 errors

## 通知系统实现（2026-05-27）

### 新增文件
- `packages/apps/web/src/lib/notification-store.ts`：React Context + useReducer 通知状态管理，50 条上限，localStorage 持久化，addNotification 自动触发 sonner toast
- `apps/desktop/src-tauri/src/commands/notification.rs`：Rust `push_notification` 命令，通过 Tauri Emitter 推送到前端
- `packages/apps/web/src/components/shell/NotificationBell.tsx`：铃铛按钮 + Popover 通知历史列表，未读 Badge 计数
- `packages/apps/web/src/components/shell/NotificationDetail.tsx`：通知详情弹窗（Dialog），显示完整消息+来源+时间
- `apps/desktop/src/ipc/notification.ts`：前端 IPC 封装，`pushNotification()` 调用 Tauri 命令
- `docs/specs/notification-system.md`：实现计划文档

### 修改文件
- `apps/desktop/src-tauri/src/commands/mod.rs`：注册 `pub mod notification`
- `apps/desktop/src-tauri/src/lib.rs`：注册 `commands::notification::push_notification`
- `packages/apps/web/src/components/shell/Topbar.tsx`：死 Bell 按钮替换为 `<NotificationBell />`
- `packages/apps/web/src/components/shell/AppShell.tsx`：包裹 `<NotificationProvider>`，新增 `NotificationBridge` 组件监听 Tauri 事件
- `packages/apps/web/src/lib/chat/gateway-ws.ts`：新增 `event=notify` 处理分支，`_handleNotifyEvent` 方法
- `packages/apps/web/src/lib/chat/types.ts`：新增 `GatewayNotifyEvent` 接口

### 验证
- tsc --noEmit: 0 errors
- cargo check: passed
- pnpm tauri build: success（产物: artifex-nexus-desktop.exe + NSIS installer）

### 调用方式
- 外部脚本：`invoke("push_notification", { req: { title, message, type: "success", source: "..." } })`
- OpenClaw cron：Gateway WS 发送 `{"event":"notify","payload":{...}}`

## Skill 合规检查器修复 + 交叉对照（2026-05-27）

### 修复内容

**manifest.json — 删除纯知识型 Skill 的 entry_point：**
- nexus-skill-manage、nexus-tool-creator、nexus-tool-executor：删除 `"entry_point": "__init__.py"`

**ue57_material_node_edit / ue57_get_material_nodes SKILL.md：**
- 补 `version` + `author` 到 metadata.artifex_nexus 块

**skill-compliance-checker main.py 优化：**
- `_check_init_py`：DCC 装饰器白名单（@ue_tool/@tool/@artclaw_tool），不再误报 UE Skill
- `_check_artifex_nexus_metadata`：新增 software(error)/author(warning) 必需检查；version→error；risk_level→可选

**nexus-skill-manage SKILL.md 更新：**
- metadata 字段表改为必需/可选；manifest 模板去掉 entry_point；新增"装饰器规范"小节

### 交叉对照结论
- UE @ue_tool vs 平台 @skill_tool 不可互换（UE SkillHub 只认 `_ue_agent_tool` 标记，平台只认 `_artifex_skill_tool`）
- metadata 必需字段统一为 software/version/author
- tool-compliance-checker 不涉及装饰器（只查 nexus-tool 的 SDK/trigger）

### DCC 装饰器全貌 + spec/tool/skill 同步（2026-05-27）

**全 DCC 装饰器映射：**

| DCC | SkillHub | 装饰器 | 导入 |
|-----|----------|--------|------|
| general（平台） | ✅ 已实现 | @skill_tool | `from artifex_nexus.skill import skill_tool` |
| unreal_engine | ✅ 已实现 | @ue_tool | `from skill_hub import tool as ue_tool` |
| blender/maya/3ds_max 等 | 📋 规划中 | @skill_tool（预备） | 同上（SkillHub 完成后生效） |

**修改的文件（第一轮）：**
- `docs/specs/skill-system.md`：§2 表格承认 UE 分叉 + §3 改为"双轨装饰器 + 对照表"
- `docs/development/skill-authoring/README.md`：名词约定更新 + 分为平台模板/UE模板
- `docs/tasks/review/STORY-0042-m4-sdk-decorator-manifest.md`：新增 PM 决策 4-6（双轨体系）
- `skills/official/nexus-skill-manage/SKILL.md`：装饰器表扩展为完整 10 DCC 映射
- `tools/official/skill-compliance-checker/main.py`：新增 software-装饰器一致性检查（3条规则）

### 架构澄清：SkillHub 作用 + 装饰器分层（2026-05-27 #2）

**SkillHub = 服务注册中心**。加载 __init__.py → 装饰器触发注册 → AI 按名调用 execute_skill()
**两条执行路径**：SkillHub 注册调用（稳定工具）vs run_python 直接执行（定制脚本）

**修正：其他 DCC 从"❌ 纯知识型"改为"📋 规划中"**，装饰器可提前写入，SkillHub 完成后自动生效

**装饰器使用决策**：
- 稳定高频工具（查询/获取信息/通用编辑）→ 写装饰器 → SkillHub 按名调用
- 定制一次性脚本 → 不写装饰器 → AI 通过 run_python 执行

**修改的文件（第二轮）：**
- `docs/specs/skill-system.md`：§3.3 改为规划中 + 执行路径对比；新增 §3.5 决策树
- `docs/development/skill-authoring/README.md`：重写装饰器作用 + 决策指南
- `skills/official/nexus-skill-manage/SKILL.md`：同上
- `tools/official/skill-compliance-checker/main.py`：规则3 warning→info（规划中DCC装饰器可保留）
- `docs/tasks/review/STORY-0042-...md`：新增PM决策7-9（装饰器分层）
- `skills/official/nexus-skill-manage/SKILL.md`：装饰器表扩展为完整 10 DCC 映射
- `tools/official/skill-compliance-checker/main.py`：新增 software-装饰器一致性检查（3条规则）

**检查器新增规则：**
1. UE + @skill_tool → error：software=unreal_engine 但 __init__.py 使用 @skill_tool
2. 非UE + @ue_tool → warning：software 不含 unreal_engine 但用了 UE 专有装饰器
3. 无SkillHub DCC + @skill_tool → warning：目标 DCC 无运行时，装饰函数不会被加载

**未修改：**
- tool-compliance-checker：不涉及装饰器检查
- nexus-tool-creator：Nexus-Tool 使用 SDK 脚本模板，不使用 Skill 装饰器

## Python 通知桥接（2026-05-27）

### 新增
- `scripts/artifex_notify.py`：Python CLI + API 双模式通知发送工具（写 JSON 到 `~/.artifexnexus/pending_notifications/`）
- `notification.rs` 新增 `scan_pending_notifications` 命令：扫描 pending_notifications/ 目录，解析 JSON → emit event → 删除文件
- `NotificationBridge` 新增 `setInterval` 轮询（每 3s 调用 `scan_pending_notifications`），消费 Python 写入的文件通知

### Python 调用方式
```bash
python scripts/artifex_notify.py --title "备份完成" --message "3个文件" --type success --source script:backup
```
```python
from scripts.artifex_notify import send_notification
send_notification(title="完成", message="OK", type="success")
```

### 验证
- tsc --noEmit: 0 errors
- cargo check: passed
- pnpm tauri build: success

## Nexus-Tool 执行完成自动通知（2026-05-27）

### 注入点
`nexus_tool_rpc.py` 中的 `_send_tool_completion_notification()` + 6 处调用：
- `_run()` 闭包：done (success) / Exception (error) / BaseException (error)
- `_install_and_run()` 闭包：dep install failed / run done / run except

### 工作方式
工具执行完成 → `_send_tool_completion_notification()` → 写 JSON 到
`~/.artifexnexus/pending_notifications/ntool_*.json` → 前端 3s 轮询消费 → toast + 铃铛

### 验证
- Python AST: OK
- pnpm tauri build: success

## Skill: nexus-openclaw-guide (2026-05-27)

### 内容
为 OpenClaw Agent 提供 Artifex Nexus 集成指南的 Skill：
- `SKILL.md`：通知 API 三通道说明、cron 任务回复配置规则（current+agentTurn）、平台特殊规则
- `api-reference.md`：push_notification / scan_pending_notifications / Gateway WS notify / NotificationStore 完整 API 文档
- `manifest.json`：general 类型，tags: openclaw/agent/api/cron/notification/integration

### 关键规则
- Cron 任务：使用 `sessionTarget: "current"` + `mode: "agentTurn"`，在 webchat 会话中执行修改
- 通知三通道：Python 文件桥接（外部脚本） / Gateway WS（cron） / Tauri IPC（桌面内）

### 安装
- 源码：`skills/official/nexus-openclaw-guide/`
- 已安装：`~/.artifexnexus/.openclaw/workspace/skills/nexus-openclaw-guide/`
- 合规检查：通过（skill-compliance-checker，本 Skill 零问题）

## Skill 重构：nexus-agent-guide 分层结构 (2026-05-27)

### 变更
- **重命名**：nexus-openclaw-guide → nexus-agent-guide（去掉 OpenClaw 限定，面向所有 Agent）
- **改为分层文档结构**：入口索引 + 按主题拆分子文档 + API 参考分离

### 新结构
```
nexus-agent-guide/
├── SKILL.md                     # 入口：规则索引 + 读取策略 + 场景速查表
├── manifest.json
├── rules/
│   ├── notifications.md         #  通知规则概述 + 通道选择决策树
│   ├── notifications-python.md  #  通道 A：Python 文件桥接
│   ├── notifications-gateway.md #  通道 B：Gateway WebSocket
│   ├── notifications-tauri.md   #  通道 C：Tauri IPC
│   └── cron-reply.md            #  Cron 回复配置规则
└── api-reference.md             #  API 精确参数参考
```

### 读取策略
Agent 先读 SKILL.md（~40 行索引）→ 按需加载 1-2 个子文档（各 ~30-50 行），而非一次性吞入 230 行单文件。

### 同步更新
- nexus-skill-manage 新增"Agent 导向 Skill 的分层文档策略"章节
- 旧 nexus-openclaw-guide 源码 + workspace 均已清理

### 验证
- skill-compliance-checker: 零问题
- pnpm tauri build: success

### 全平台统一 @skill_tool（2026-05-27 #3 彻底统一）

**装饰器唯一源**：`packages/dcc/shared/artifex_nexus_sdk/decorator.py`
**发现机制**：所有 Hub 统一 walk `module.__dict__` for `_artifex_skill_tool = True`

**改了什么：**
- 共享 decorator 创建 + UE 部署 (`Content/Python/artifex_nexus_sdk/`)
- Platform `core.py` → re-export from shared
- UE `skill_hub.py`: 移除 `tool()` 装饰器，新增 `_collect_decorated_from_module()`
- 4 UE Skill（10+ 函数）: `@ue_tool` → `@skill_tool`
- 合规检查器: `@ue_tool` → error（已废弃）
- 文档: skill-system.md / skill-authoring / dcc-plugin-management / STORY-0042 / nexus-skill-manage 全量同步
- `@ue_tool` / `@artclaw_tool` / `@tool` 在所有活跃代码中已清除

## 全平台 SkillHub 基建完成（2026-05-27 #4 接入 Blender/Maya/Max）

### 新建文件
- `packages/dcc/shared/artifex_nexus_sdk/skill_manifest.py` — manifest 解析与验证（从 UE 提取，零 DCC 依赖）
- `packages/dcc/shared/artifex_nexus_sdk/skill_hub.py` — 共享 SkillHub 核心（全平台统一）

### 修改文件

**共享 SDK：**
- `packages/dcc/shared/artifex_nexus_sdk/__init__.py` — 导出 SkillHub / SkillManifest

**UE（切换为共享 skill_manifest）：**
- `packages/dcc/unreal/.../skill_hub.py` — `from artifex_nexus_sdk.skill_manifest import ...`
- `packages/dcc/unreal/.../skill_version.py` — 同上
- `packages/dcc/unreal/.../skill_conflict.py` — 同上
- `packages/dcc/unreal/.../skill_mcp_resources.py` — 同上
- `packages/dcc/unreal/.../skill_mcp_tools.py` — 同上 + 模板代码 `@ue_tool` → `@skill_tool`

**Blender:**
- `__init__.py` — 新增 `_skill_hub` / `_init_skill_hub()` / `_get_skill_hub()`，在 MCP Server 启动后 + 手动启动时初始化 SkillHub，`unregister()` 时停止监控
- `mcp_server.py` — run_python 描述中新增 SkillHub 调用示例

**Maya:**
- `__init__.py` — 同上 + 新增共享 SDK 路径注入（`_sdk_probe`）；`stop_server()` + `unregister()` 清理 SkillHub
- `mcp_server.py` — 同上

**3ds Max:**
- `__init__.py` — 同上 pattern；`stop_server()` + `unregister()` 清理 SkillHub
- `mcp_server.py` — 同上
- `startup.py` — 新增 `_an._init_skill_hub()` 调用

**文档：**
- `docs/specs/dcc-plugin-management.md` — §1 架构图更新（新增 decorator/skill_manifest/skill_hub）；§3.5 SkillHub 运行时状态表全量更新
- `skills/official/nexus-skill-manage/SKILL.md` — 全 DCC 装饰器对照表：Blender/Maya/Max 改为 ✅ 已实现

### 架构
```
共享 SDK (skill_hub.py + skill_manifest.py + decorator.py)
├── Blender: __init__.py → _init_skill_hub() → scan + watch
├── Maya:    __init__.py → _init_skill_hub() → scan + watch
├── Max:     __init__.py + startup.py → _init_skill_hub() → scan + watch
└── UE:      skill_hub.py (独立实现) → scan + watch (polling/DirectoryWatcher)
```

### AI 调用方式（所有 DCC 统一）
```python
from artifex_nexus_sdk.skill_hub import get_skill_hub
hub = get_skill_hub()
hub.execute_skill("skill_name", {"arg": "value"})
hub.list_skills()
```

### 验证
- Python 语法检查：3 个新文件全部通过
- `@ue_tool` / `_ue_agent_tool` / `from skill_hub import tool` 在活跃代码中 0 残留
- `skill_hub.py` 共 2 个文件（共享 SDK + UE 独立实现）
- `skill_manifest.py` 共 2 个（共享 SDK + UE 本地副本，UE 代码已改为 import 共享版）
- 共享 `skill_manifest.py` 新增 `blender` 到 VALID_SOFTWARE

## OpenClaw 配置预设：tools.sessions.visibility

- 在 `bootstrap.py::_generate_default_config()` 新增 `tools.sessions.visibility = "all"` 预设
- 效果：安装/重装后，OpenClaw 默认所有 session 对 agent 可见
- 影响文件：`packages/adapters/openclaw/wrapper/src/artifex_nexus/openclaw_wrapper/bootstrap.py`

## QA 审查：SkillHub 基建代码（2026-05-27 #5）

### P0 修复（3 个）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `UE skill_hub.py:41` | `scan_skill_dir` 从共享 `skill_manifest` 导入但函数不存在（仅 UE 本地有），且实际未使用 → ImportError | 从 import 中移除 `scan_skill_dir`（死代码） |
| 2 | `shared skill_hub.py:625` | `_manifest_from_skill_md` 中 `fm_name = canonical_name if fm_name != canonical_name else fm_name` 永远使用目录名覆盖 frontmatter name | 改为以 frontmatter name 为准，不一致时仅打日志 |
| 3 | `Blender __init__.py` | `_init_skill_hub()` 使用 `os.path.join()`/`os.path.expanduser()` 但未 `import os` → NameError | 添加 `import os` |

### P1 修复（3 个）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 4 | `shared skill_hub.py` | `__init__` 中 `_watch_enabled`/`_watch_interval`/`_file_mtimes`/`_poll_thread` 未初始化，`stop_watching()` 调用早于 `start_watching()` 会 AttributeError | 在 `__init__` 中初始化所有 watcher 状态变量 |
| 5 | `shared skill_hub.py` | `stop_watching()` 未 join `_poll_thread`，线程可能残留 | 添加 `_poll_thread.join(timeout=3.0)` |
| 6 | `shared skill_hub.py` | `start_watching()` 用 `_poll_handle is not None` 防重入，但 `_poll_handle` 从未赋值（死变量） | 改为 `_poll_thread.is_alive()` 防重入 |
| 7 | `shared skill_hub.py` | `disable_skill()` 不清理 `_loaded_modules`/`sys.modules`，同 session re-scan 无法重载 | 添加模块卸载逻辑 |

### P2 修复（1 个）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 8 | `shared skill_hub.py:43` | `from artifex_nexus_sdk.decorator import skill_tool, SkillToolResult` — 死导入（文件中未使用） | 移除 |

### P2 已知（不修复，暂接受）

| # | 文件 | 问题 | 原因 |
|---|------|------|------|
| K1 | `skill_hub.py:830` | `auto_name()` hardcode `ue57_` 前缀 | UE 版本前缀，暂时合理 |
| K2 | `skill_hub.py:132` | YAML frontmatter 解析不支持嵌套/多行/引号内冒号 | 简单 parser，够用 |
| K3 | `skill_hub.py:815` | `enable_skill()` stub 返回 False | 预留接口 |
| K4 | `skill_hub.py:262` | `DCC_SUBDIRS` 含 `"unreal"` 但 `dcc_name` 是 `"unreal_engine"` | 仅目录扫描用，不影响功能 |

### 验证

- 7 个修改文件全部通过 Python `ast.parse` 语法检查
- `VALID_SOFTWARE` 与所有 DCC 的 `dcc_name` 完全匹配
- `scan_skill_dir` 仅存在于 UE 本地 `skill_manifest.py`，不影响共享版
- `os` 导入：Blender/Maya/Max 全部就绪

## COMPACTED HISTORY 后消息接收修复（2026-05-27）

### 问题
OpenClaw Gateway 在上下文超限时触发 auto-compaction（总结历史 → retry prompt），compaction 后的回复事件不再通过 WebSocket 推送到前端，导致用户看不到后续回复。

### 修复（仅 `chat-service.ts`）

1. **`syncFromGateway()`** — 新增函数：通过 `ws.sendRpc("chat.history")` 拉取会话最新 10 条消息，对比本地消息 ID 去重，`dispatch MERGE_MESSAGES` 合并缺失消息到 state + messageCache

2. **触发点（3 处）**：
   - `handleGatewayEvent` final case：每次 final 后异步调用 syncFromGateway
   - `handleGatewayEvent` error/aborted case：同上（可能 compaction 成功但前端收到 abort）
   - `ws.onStateChange`：WS 从 disconnected 恢复为 connected 时调用（断连重连补齐）

3. **`MERGE_MESSAGES`** — 新增 reducer action：按 ID 去重，按时间戳排序合并

4. **processQueue 延迟**：final 后 `setTimeout(200)` 替代 `setTimeout(0)`，给 syncFromGateway 时间补齐

### 验证
- TypeScript 编译：0 errors

### QA 审核后修复（2026-05-27）

P1-1: `loadHistoryMessages` 重复解析逻辑 → 委托给 `_parseHistoryMessages` 统一解析
P1-2: `syncFromGateway` 无并发保护 → 新增 `syncInFlightRef` + finally 重置
P2-1: 硬编码 `limit=10` → 提为模块常量 `SYNC_FETCH_LIMIT=20`
P2-2: 补齐缺少状态追踪 → 新增 triggered/skipped/done 日志

# 2026-05-16

## STORY-0044 SkillInstaller 完成 + hasattr 重复消除

### software_value() 提取（Task #17）
- 在 `categories.py` 添加 `software_value(software: Software | str) -> str` 统一入口
- 消除 3 处重复 `hasattr(s, 'value')` 模式：`hub/core.py:74`、`hub/instance.py:55`、`registry.py:58`
- 加入 `__init__.py` 公共 API 导出

### Phase 1 遗留 Bug 修复
- **`@skill_tool` 无括号不支持**：装饰器增加 `func=None` 首位参数 + `*` 强制 keyword-only，支持 `@skill_tool` / `@skill_tool()` / `@skill_tool(name="x")`
- **`SkillToolResult.ok()` 缺失**：添加 `ok()` 作为 `success()` 便捷别名

### STORY-0044 SkillInstaller 实现
- 文件：`installer.py`（~400 行）
- 类：`SkillInstaller`（组合 SkillHub）
- 方法：`install()` / `uninstall()` / `sync()` / `sync_all()` / `publish()` / `enable()` / `disable()` / `is_enabled()` / `toggle()` / `get_disabled_skills()`
- 数据类：`InstallResult` / `SyncResult` / `PublishResult`
- `SkillConfig`：轻量 JSON 配置（`~/.artifexnexus/config/skills.json`），管理 enable/disable + 预留 pin/favorite

### SkillHub 增强
- 添加公开属性 `skills_root` / `layer_sources`（之前为私有 `_skills_root` / `_layer_sources`）

### 测试
- software_value: 6/6 通过（含 @skill_tool 无括号 + SkillToolResult.ok 验证）
- installer: 10/10 通过（install/re-install-sync/sync/source-change-sync/enable-disable/publish/uninstall/error-cases/sync_all）
- 全量回归 Phase 1+2+3: 全部通过
- pip install -e: 成功

## STORY-0044 PM→开发→QA 三道关卡补走

### PM 关卡（审核 4 个设计问题）
- **SkillLoader**：确认不独立为类，并入 Hub 内部两阶段（扫描元数据 + 懒加载 @skill_tool）。遵循 ADR 0003 "tool 用到再加载"。
- **pin/favorite**：从 SkillInstaller 移除。它们是用户偏好不是安装操作，放到 SkillConfig。
- **SkillConfig 位置**：提取到 `packages/platform/core/src/artifex_nexus/core/skill_config.py`，独立文件。
- **线程安全**：单进程串行场景，原子 rename 已足够，不加文件锁。

### 开发关卡
- 创建 `core/skill_config.py`（163 行）：含 enable/disable、pin/unpin、favorite/unfavorite 三组共 12 个公开方法
- 从 `installer.py` 移除内嵌 SkillConfig 类（-70 行），改为 `from artifex_nexus.core.skill_config import SkillConfig`
- 清理 installer.py 死导入：移除 `software_value`、`load_manifest_model`
- `skill/__init__.py`：SkillConfig 导入源改为 core 包
- `core/__init__.py`：导出 SkillConfig

### QA 关卡
- 全量检查通过：行数 ≤500、函数 ≤80 行、无死代码、导入链完整
- SkillConfig 12 方法全验证、SkillInstaller 确认无 pin/favorite

### SDD 状态迁移（三处同步）
- 文件：`backlog/` → `ready/` STORY-0044
- frontmatter.status：`backlog` → `ready`
- board.md：Backlog 列移除 STORY-0044，Ready 列新增

## STORY-0042 PM→开发→QA 三道关卡

### PM 关卡（调研 OpenClaw + 设计审核）
- OpenClaw 看不到 Skill 内部 skill-tool 名
- **namespace 结论**：Hub `_collect_tools_from_module()` 已做 per-Skill tools dict 隔离，全局 `_TOOL_REGISTRY` 是死代码 → 移除
- **SkillToolInfo 定义**：在 `hub/instance.py` 新增轻量 dataclass（name/description/category/risk_level/input_schema），面向公共 API

### 开发关卡
- `hub/instance.py`：新增 `SkillToolInfo` dataclass（+19 行）
- `decorator/core.py`：移除 `_TOOL_REGISTRY` 全局 dict、`get_registered_tools()`、`is_tool()`、`get_tool_name()`、`_ue_agent_tool`/`_ue_agent_tool_name` 标记（-53 行，243→190 行）
- `decorator/__init__.py`：移除 3 个死导出
- `hub/__init__.py`：导出 SkillToolInfo
- `skill/__init__.py`：导出 SkillToolInfo

### QA 关卡
- 导入链全量验证通过：`@skill_tool` 仍设置 `_artifex_tool`/`_artifex_tool_name` 标记
- `_collect_tools_from_module()` 正常工作
- `_ue_agent_tool` artclaw 残留已清除
- 所有文件 ≤ 500 行

### SDD 状态迁移
- backlog/ → ready/ STORY-0042

## STORY-0043 PM→开发→QA 三道关卡

### PM 关卡（深度调研）
- **问题 1 — 版本函数三处重复**：`_parse_version_tuple` + `_compare_versions` 在 version/parser.py、registry.py、conflict/detector.py 各一份。
  根因：artclaw 的 version_manager.py 是单体文件，v2 计划拆成三个模块时连带拷贝并误标"避免循环导入"。
  验证：version/parser.py 无任何 skill 子模块依赖 → 不存在循环依赖。
- **问题 2 — load_skill/execute_tool 是否需要**：
  - 管理路径（Sidecar）只走 scan_all_skills()（manifest.json），不需要 Python import
  - 执行路径（DCC 内，通过 run_python）AI 代码可能需要 load_skill/execute_tool
  - 当前无外部调用者但设计合理，保留不删
- **下游风险发现**：STORY-0045（run_tool）→ 0046（tool.run RPC）→ 0047（[▶ 运行] 按钮）构成 tool 执行通过 sidecar 的错误链路。
  已在 3 个下游 STORY 文件中添加 PM 标注，建议从 Sidecar RPC 移除 tool.run。

### 开发关卡
- registry.py：移除内联版本函数（-43 行），改为 `from .version.parser import compare_versions, parse_version`
- conflict/detector.py：移除内联版本函数（-56 行），同上
- 两文件共消除 ~99 行重复代码

### QA 关卡
- 导入链验证通过、版本比较/matches/conflict 功能正常、Hub scan 正常

### SDD 状态迁移
- backlog/ → ready/ STORY-0043
- board.md：Backlog 移除 0043，Ready 新增 0043
- PM 决策写入 STORY-0043
- frontmatter.status：backlog → ready + PM 决策写入
- board.md：Backlog 移除 STORY-0042，Ready 新增

## 全量命名替换：tool → Skill-Tool / Nexus-Tool

### 问题
"tool" 一词在项目中指代两个不同概念：
- **Skill-Tool**：Skill 包内 @skill_tool 装饰的函数，由 OpenClaw 运行（通过 run_python）
- **Nexus-Tool**：Artifex 独立 Tool 模块（ToolRegistry/ToolInstaller），由 Artifex sidecar 管理+运行

### Python 代码重命名
- `@tool` → `@skill_tool`（保留 `@tool` 和 `@artclaw_tool` 作为兼容别名）
- `ToolResult` → `SkillToolResult`
- `ToolRef` → `SkillToolRef`
- `ToolInfo` → `SkillToolInfo`
- `execute_tool` → `execute_skill_tool`
- `_artifex_tool` → `_artifex_skill_tool`
- `_artifex_tool_name` → `_artifex_skill_tool_name`
- `tool_names` → `skill_tool_names`
- manifest.json `"tools"` → `"skill_tools"`
- `execute()` → `execute_skill_tool()`

### 文档重命名
- STORY-0045: `STORY-0045-m5-sdk-tool-registry` → `STORY-0045-m5-sdk-nexus-tool-registry`
- STORY-0046: `STORY-0046-m4-rpc-skill-tool` → `STORY-0046-m4-rpc-skill-nexus-tool`
- STORY-0047: `STORY-0047-m4-ui-skill-tool-wiring` → `STORY-0047-m4-ui-skill-nexus-tool-wiring`
- STORY-0048: `STORY-0048-m4-migration-skill-tool` → `STORY-0048-m4-migration-skill-nexus-tool`
- EPIC-0005: `EPIC-0005-m5-tool-system` → `EPIC-0005-m5-nexus-tool-system`
- ToolRegistry → NexusToolRegistry, ToolInstaller → NexusToolInstaller
- `tool.run` RPC → `nexus-tool.run`（等所有 RPC 方法）
- `~/.artifexnexus/tools/` → `~/.artifexnexus/nexus-tools/`
- board.md 全部引用已更新

### 验证 + 补漏修复（2026-05-16 第二轮）
验证发现 7 处遗漏的过期引用，已全部修复：
- **testing/phase2_integration_test.py**（6 处，运行时关键）：`ToolResult`→`SkillToolResult`、`tool`→`skill_tool`、`execute_tool`→`execute_skill_tool`、`tool_names`→`skill_tool_names`、`execute()`→`execute_skill_tool()`、manifest `"tools"`→`"skill_tools"`
- **docs/development/skill-authoring/README.md**（4 处，用户文档）：`@tool`→`@skill_tool`、`ToolResult`→`SkillToolResult`、`Tool`→`SkillTool`
- **docs/specs/skill-system.md**（8 处，设计文档）：`@tool`→`@skill_tool`、`ToolResult`→`SkillToolResult`、`execute()`→`execute_skill_tool()`、`Tool`→`SkillTool`
- **docs/specs/openclaw-agent-preset.md**（2 处）：`tool, ToolResult`→`skill_tool, SkillToolResult`、`@tool`→`@skill_tool`
- **packages/platform/skill/src/.../manifest/models.py**（1 处，docstring）：`ToolRef`→`SkillToolRef`
- **STORY-0045**（1 处）：`ToolResult`→`NexusToolResult`
- 研究文档（research/）中的历史引用**未修改**（历史快照，不影响运行）

## STORY-0045 NexusToolRegistry + NexusToolInstaller 完成（PM→开发→QA）

### PM 关卡
- 调查 artclaw Tool 运行机制：Tool ≠ SkillTool，Tool = 目录（manifest.json + main.py），三种执行路径（subprocess / MCP 注入 / AI 驱动）
- 决策：`run_nexus_tool` 保留为 SDK 方法，不从 Sidecar RPC 暴露；Pin/Favorite 复用 SkillConfig（加 nexus_tools 段）；触发器推后到 M6+
- 命名铁律：所有 Nexus-Tool 相关名称必须包含 nexus/nexus_tool，零裸 tool

### 开发关卡
- 新建 `nexus_tool/` 子包（5 文件，共 822 行）：
  - `models.py`（75 行）：ScannedNexusTool, NexusToolData, NexusToolResult
  - `scanner.py`（156 行）：scan_nexus_tools()
  - `registry.py`（263 行）：NexusToolRegistry（发现/查询/启停/运行）
  - `installer.py`（300 行）：NexusToolInstaller（CRUD/Publish/Pin/Favorite）
  - `__init__.py`（28 行）：公共 API
- 扩展 `SkillConfig`（+87 行）：新增 nexus_tools 段，12 个方法
- 更新 `skill/__init__.py`：导出 6 个 nexus_tool 公共 API

### QA 关卡
- 13 项功能测试全部通过
- 命名检查：零裸 tool ✅
- 行数：全部 ≤300 ✅
- 验收标准 18/18 覆盖 ✅

### SDD 状态
- STORY-0045 frontmatter.status：backlog → ready
- board.md：Backlog 移除 → Ready 新增

## STORY-0046 Sidecar RPC：Skill/Nexus-Tool 方法注册（PM→开发→QA）

### PM 关卡
- 审核 STORY-0046 验收标准与已有 SDK API 映射
- 确认 nexus-tool.run 移除（PM标注），保留为 SDK 方法不暴露 RPC
- 决策：filter 使用平铺参数、RPC 层分页、detail 复合结构、enable/disable 走 SkillInstaller
- 实际方法数：14 skill + 13 nexus-tool = 27（STORY 标题数字不精确，以 AC 为准）

### 开发关卡
- 新建 3 个文件（拆分为 ≤500 行）：
  - `_rpc_helpers.py`（181 行）：懒初始化单例 + 工具函数
  - `skill_rpc.py`（473 行）：14 个 skill.* handler + SKILL_METHODS dict
  - `nexus_tool_rpc.py`（351 行）：13 个 nexus-tool.* handler + NEXUS_TOOL_METHODS dict
- 修改 `sidecar.py`：+2 import + METHOD_TABLE 追加 27 条目

### QA 关卡
- 文件行数：全部 ≤500 ✅（181/473/351）
- 函数行数：全部 ≤80 ✅（最长 68 行）
- 命名铁律：零裸 tool ✅
- 导入链：无循环依赖 ✅
- Error handling：全部 JSON-RPC 标准错误码（-32602 params, -32000 general）
- Smoke tests：全部通过

### SDD 状态
- STORY-0046 frontmatter.status：backlog → ready，AC 全部 ✅
- board.md：Backlog 移除 → Ready 新增
- PM标注修正：nexus-tool.run 保留（MCP Bridge 路由 + subprocess）

## STORY-0047 Web UI：Skill/Nexus-Tool 管理面板接线（PM→开发→QA）

### PM 关卡
- 重新理解 nexus-tool.run 执行路径：DCC 工具 → Sidecar MCP Bridge → DCC run_python，通用工具 → subprocess
- 修正在 STORY-0046 中错误移除 nexus-tool.run 的决定，补回 handler
- 确认：D5 重构一并做；UI "Tool" → "Nexus-Tool" 命名修正
- 用户决策：Q1→合并到 0047 一起做, Q2→全量, Q3→按铁律改名

### 开发关卡
- 补回 nexus-tool.run RPC handler（nexus_tool_rpc.py +109 行）：DCC↔MCP 映射表 + MCP Bridge 路由 + subprocess 通用执行
- 新建 TypeScript API：
  - `lib/skill/skill-api.ts`（183 行）：14 个 skill API 方法 + SkillItem/SkillDetail 类型
  - `lib/nexus-tool/nexus-tool-api.ts`（184 行）：14 个 nexus-tool API 方法 + NexusToolItem 类型
- 新建 Rust Tauri commands：`commands/skill.rs`（379 行）：28 个 #[tauri::command]
- 修改 `lib.rs` + `commands/mod.rs` 注册新 module
- UI 接线：
  - SkillList.tsx 重写：mock→skill-api，loading/error 状态，实时操作
  - NexusToolList.tsx 新建（替代 ToolList.tsx）：mock→nexus-tool-api，[▶ 运行] 按钮
  - SkillsPage.tsx：Tab "Tool" → "Nexus-Tool"
  - AppShell.tsx：新增 PreviewContext（setPreview/clearPreview）
  - RightPanel.tsx：D5 接入 PreviewContext + PreviewRenderer（kind→组件）
- 修正 STORY-0046/0047 过期 PM标注

### QA 关卡
- 文件行数：全部 ≤500 ✅（最长 nexus_tool_rpc 475 行）
- 命名铁律：零裸 "tool" ✅
- METHOD_TABLE：14 skill + 14 nexus-tool（含 run）✅
- TS invoke ↔ Rust command 全部对应 ✅
- 容错：loading/error 状态 + 单个操作不影响列表 ✅

### SDD 状态
- STORY-0047 frontmatter.status：backlog → ready，AC 全部 ✅
- board.md：Backlog 移除 → Ready 新增
- STORY-0046 PM标注修正为正确方案

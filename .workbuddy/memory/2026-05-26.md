# 2026-05-26

## Maya/Max 插件启动链修复 + Gateway Plugin 独立安装行 + Tauri 集成

### Gateway Plugin
- **根因**: index.js (May 24) 是在 index.ts (May 25) 更新 Maya/Max 之前编译的 → 不含 maya-primary/max-primary 代码
- **修复**: 重编译 index.js（esbuild）→ 20.9KB，确认含 4 个 DCC server
- 安装向导新增「Gateway Plugin」独立行（fixture `gateway-plugin`，GP 标签）
- 移除 UE 安装中 L1282-1287 的 gateway 部署代码
- package.json 添加 build script，tauri.conf.json beforeBuildCommand 中自动编译
- 新增 uninstall RPC 全链路：Python handler + Rust command + TS function
- 已部署到 OpenClaw extensions，清除 stale dist/

### Maya 插件
- `register()` 改为 `maya.utils.executeDeferred(_deferred_startup)` 延迟启动
- `generate_user_setup()` 修正路径注入（相对路径 + maya_version 参数）
- `install_maya_addon()` 新增 `_deploy_maya_user_setup()` 自动部署 userSetup.py
- 部署：首创文件 / 追加代码段 / locale 同步

### 3ds Max 插件
- **startup.py 重写**: `_main()` → `QTimer.singleShot(2000, _deferred_startup)` + `_startup_done` 进程锁
- **_create_menu() 重写**: `macroScript` 注册 + `createActionItem` 引用（artclaw 模式）
- **max_adapter.py**: `#timeout` 回调 → `QTimer(50ms)` 轮询（`_start_poll_timer` + `_pump_tasks`），QTimer 不可用时回退 `#timeout`
- `register()` 移除自动启动逻辑（统一由 startup.py 管理）

### 文档
- 新建 `docs/development/dcc-plugin-development-guide.md`（Maya/Max 启动链、菜单注册、主线程调度、Gateway 插件、常见陷阱）

### 构建
- Next.js ✓ | Gateway plugin esbuild ✓ | Rust ✓ 1m19s | NSIS ✓
- Commit b9481e2 → pushed to main

## QA 审核修复（10:15-10:33）

**复核结果**: P0×3 需修复 + P1×3 需修复 + P2×3 需修复 + P1×1 放行

### 根因发现
- Maya/Max 安装后目录结构是**扁平**的: `scripts/artifex_nexus/__init__.py`（无 v2023/maya_addon/ 子目录）
- userSetup.py/startup.py 生成的导入路径引用不存在的嵌套目录 → Maya/Max 无法加载

### P0 修复
- [P0#1] Maya userSetup.py: `import artifex_nexus; artifex_nexus.register()`（Maya 自动加 scripts/ 到 Python 路径）
- [P0#2] Max startup.py: 同时加 scripts/ 和 scripts/artifex_nexus/ 到 sys.path
- [P0#3] Maya plugin_info 加 `maya_min: (2023,)`

### P1 修复
- [P1#4] Blender 安装移除冗余 gateway deploy
- [P1#6] Gateway Plugin 新增 Trash2 卸载按钮（含确认弹窗）
- [P1#7] Max startup.py 设置 max_adapter._global_adapter

### P2 修复
- [P2#8] Max 移除 v2023 硬编码: macroScript import + startup.py import 统一用扁平路径
- [P2#9] Maya _shelf_start 加 show_alert 参数
- [P2#10] generate_user_setup + register() docstring 统一

### 放行
- [P1#5] upToDate 已有 hash 比较，无需修复

**构建**: Next.js ✓ | Gateway esbuild ✓ | Rust ✓ 1m48s | NSIS ✓
**提交**: f59da98 → pushed to main

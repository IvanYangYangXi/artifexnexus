# 2026-05-20

## 发布确认弹窗 + 移除实现方式字段（任务 #1→#2→#3 全链路）

### 任务 #1: 探索发布流程（已完成）
- Tool publish: `NexusToolList → API → Rust → Python RPC → Installer.publish_nexus_tool()` — 复制文件 + 更新 manifest
- Skill publish: `SkillList → API → Rust → Python RPC → Installer.publish()` — metadata-only
- 模板 `template.json` 不含 `implementation_type` ✅

### 任务 #2: 发布确认弹窗 + 字段校验
**新建文件**：
- `PublishConfirmDialog.tsx` — 支持 "tool"/"skill" 两种模式
  - Tool: 收集 target（official/marketplace）、version、description（含 semver 格式校验）
  - Skill: 收集 target_layer（team/official）、展示 name/version 摘要
  - 通用：open/onClose/onConfirm pattern, busy 状态

**修改文件**：
- `NexusToolList.tsx` — 发布按钮从直接 API 调用改为打开 Dialog
- `SkillList.tsx` — 同上

### 任务 #3: 移除"实现方式"字段 + 模板单一源
**前端（5 文件）**：
- `nexus-tool-api.ts` — 移除 `NexusToolItem.implementation_type`、`CreateOptions`、`UpdateOptions`、`SaveAsInstanceOptions` 中的字段，创建函数不再发送
- `NexusToolList.tsx` — 移除 `IMPL_LABELS` 常量和卡片显示
- `ToolDetailPanel.tsx` — 移除 `IMPL_LABELS`、`editedImplType` state、InfoTab props、UI 编辑下拉
- `RunPanel.tsx` — 移除信息行显示
- `skillsMock.ts` — 移除 `ImplType` 类型、`IMPL_LABELS` 常量、`MockTool.implType` 字段，更新 6 个 MOCK_TOOLS 条目

**Python 后端（6 文件）**：
- `models.py` — 移除 `NexusToolCreateParams.implementation_type` 和 `NexusToolData.implementation_type`
- `scanner.py` — 移除 `impl_type` 变量提取和 `ScannedNexusTool` 参数
- `registry.py` — 移除 `NexusToolData` 构造中的参数
- `installer.py` — 移除 `create_nexus_tool` 签名参数，硬编码 `manifest["implementation"] = {"type": "script"}`；移除 `update_nexus_tool` 中的处理逻辑
- `nexus_tool_rpc.py` — 移除 create/update handler 中的参数传递
- `_rpc_helpers.py` — 移除序列化中的字段

### 交叉修复：预存 Bug
- `skill-api.ts` — `skillSync`/`skillPublish` 的 opts 类型从 camelCase (`sourceLayer`/`targetLayer`) → snake_case (`source_layer`/`target_layer`)，对齐 Python 后端

### 验证
- TypeScript: `tsc --noEmit` 0 错误 ✅
- Python: 6/6 文件语法检查通过 ✅
- 全量扫描：前端 + 非 bundled Python 后端 0 处 `implementation_type` 残留 ✅
- 交叉边界一致性：参数名对齐 ✅
- 模板单一源确认：`template.json` 无 `implementation_type` ✅

## 重构 _bundled_nexus_tools 目录结构（2026-05-20 07:22）

### 目标
1. 移除 DCC 分类文件夹层级（blender/unreal/universal 中间目录）
2. 将 tool-compliance-checker 和 artclaw-skill-compliance-checker 移到项目根 `tools/`

### 目录扁平化
- `marketplace/blender/*` → `marketplace/*`（2 工具）
- `marketplace/unreal/*` → `marketplace/*`（4 工具）
- `official/universal/*` → 删除（两个检查器移到 tools/）

### 代码修改（6 文件）
- `scanner.py` — `scan_nexus_tools()` 增加 `tools_path` 参数，扫描 `tools/` 下工具（source 标记为 official）
- `registry.py` — 构造函数增加 `tools_path`，传递给 scanner
- `_rpc_helpers.py` — 新增 `_get_tools_path()`（向上查找 pnpm-workspace.yaml 定位项目根），传递到 Registry
- `bootstrap.py` — `_register_default_tool_sources()` 增加 tools/ 注册
- `dcc_installer.py` — 增加 tools/ 注册
- `scripts/migrate_skills_nexus_tools.py` — 移除合规检查器条目，marketplace 路径扁平化，计数 8→6

## QA Review: 触发器→扫描→执行全链路审核（2026-05-20 10:32）

逐节点追踪 Blender 触发器完整执行路径，确认所有波及代码已同步：

### 链路验证
- Blender addon trigger_dispatcher: tool-sources.json → rglob → tool_dir ✅
- Sidecar trigger_dispatcher: 同上 ✅
- NexusToolRegistry: tools/{official,marketplace}/ 扫描 ✅
- _rpc_helpers: _get_tools_path() ✅
- nexus_tool_rpc: ntd.nexus_tool_path ✅
- NexusToolInstaller: _tools_path / target / name ✅
- SDK 注入 (Blender + Sidecar): 路径正确 ✅

### 结果
- 全局 _bundled_nexus_tools 代码引用: 0 处 ✅
- Manifest ID: 8/8 ✅
- Python 语法: 9/9 ✅
- TypeScript: 0 errors ✅
- 逻辑无冲突 ✅

### 波及安全确认
- `trigger_dispatcher.py` — 通过 tool-sources.json 发现，不依赖目录结构 ✅
- `sidecar.py` — 不直接引用 _bundled_nexus_tools 路径 ✅
- `nexus_tool_rpc.py` — 通过 registry 获取路径，不硬编码 ✅
- 前端 TS/TSX — 无硬编码路径引用 ✅
- `NexusToolInstaller` — 不需要 tools_path（tools/ 不是发布目标）✅

### 验证
- Python 语法：6/6 文件通过 ✅
- TypeScript：tsc --noEmit 0 errors ✅
- 关键词扫描：无 `official/universal`、`marketplace/unreal` 残留 ✅
- tool-sources.json：tools/ 已注册（type: tools, count: 2）✅
- 已知不影响：部分 manifest id 与目录名不一致（预存行为，scanner 运行时自动修正）

## 清理 _bundled_nexus_tools 残留引用（2026-05-20 10:25）

用户已手动完成目录迁移：`_bundled_nexus_tools/` 已删除，所有工具已移到 `tools/{official,marketplace}/`。

代码清理（用户已完成大部分）：
- `scanner.py` — `bundled_nexus_tools_path` → `tools_path`，扫描 `tools/{official,marketplace}/` ✅
- `registry.py` — 参数重命名 ✅
- `_rpc_helpers.py` — 移除 `_BUNDLED_NEXUS_TOOLS_PATH`/`_get_bundled_nexus_tools_path()`，只用 `_get_tools_path()` ✅
- `installer.py` — `_tools_path` 替换 `_bundled_nexus_tools_path` ✅
- `bootstrap.py` — 无 _bundled 引用 ✅
- `dcc_installer.py` — 无 _bundled 引用 ✅

我完成的清理：
- `sidecar.py:102` 注释更新 ✅
- `tool_sources.py:263` 注释更新 ✅
- `__pycache__` 清理 ✅

### 验证
- Python 语法：9/9 通过 ✅
- TypeScript：0 errors ✅
- Manifest ID：8/8 与目录名一致 ✅
- tool-sources.json：skills + user + tools（无 _bundled）✅
- 全局扫描：0 处 _bundled_nexus_tools 代码引用 ✅

## 重构 tools/ 为统一工具根（2026-05-20 08:37）

### 设计
`tools/{official|marketplace}/{工具名}/` — 统一工具根，取代 _bundled_nexus_tools。

### 文件迁移
- `tools/tool-compliance-checker/` → `tools/official/tool-compliance-checker/`
- `tools/artclaw-skill-compliance-checker/` → `tools/official/artclaw-skill-compliance-checker/`
- `_bundled_nexus_tools/marketplace/*` → `tools/marketplace/*`（6 工具）
- 删除 `_bundled_nexus_tools/` 整目录

### 代码修改（8 文件）
- `scanner.py` — `bundled_nexus_tools_path` → `tools_path`，移除冗余 tools_path 参数
- `registry.py` — 合并为单一 `tools_path` 参数
- `installer.py` — `_bundled_nexus_tools_path` → `_tools_path`
- `_rpc_helpers.py` — 合并 `_get_bundled_nexus_tools_path()` + `_get_tools_path()` → 统一 `_get_tools_path()`
- `bootstrap.py` — 移除 _bundled register，统一注册 tools/
- `dcc_installer.py` — 同上
- `trigger_dispatcher.py` — `_find_nexus_tools_root()` 改为 pnpm-workspace.yaml 探测
- `tool-sources.json` — 移除 _bundled 条目，tools 条目 tool_count=8

### 验证
- Python 语法：8/8 文件通过 ✅
- 8 个 manifest ID 全部与目录名一致 ✅
- _bundled_nexus_tools 已删除 ✅
- 关键词扫描：功能代码无残留 ✅

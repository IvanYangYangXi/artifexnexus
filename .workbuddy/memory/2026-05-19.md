# 2026-05-19

## 移除 manifest skill_tools 字段 + SKILL.md 文档显示

**需求**：manifest 中的 `skill_tools`/`tools` 字段没有意义，移除；工具列表分页改为直接以 Markdown 格式显示 SKILL.md 文档。

**修改文件（14 个）**：
- `template.json`：移除 `"tools": []`
- `manifest_fixer.py`：移除 tools 自动生成逻辑（含兜底模板），更新 docstring
- `models.py`：删除 `SkillToolRef` 类、`skill_tools` 字段、`_validate_skill_tools_non_empty` 校验器、`skill_tool_names` property
- `manifest/__init__.py`：移除 `SkillToolRef` 导入和导出
- `hub/core.py`：移除两处 `skill_tools=[]` 合成参数
- `_rpc_helpers.py`：`_entry_to_dict` 移除 `skill_tools` 序列化；新增 `_skill_read_skill_md()` 读取 SKILL.md 内容
- `skill_rpc.py`：移除从 manifest 读取 `skill_tools` 的代码；注册 `skill.read_skill_md` RPC
- `registry.py`：移除搜索中的 `skill_tool_names` 引用
- `manifest.schema.json`：移除 `skill_tools` schema 定义
- `skill/__init__.py`：移除 `SkillToolRef` 导入和 `__all__` 条目
- `skill-api.ts`：`SkillEntry` 移除 `skill_tools` 字段；新增 `skillReadSkillMd()` API
- `SkillDetailPanel.tsx`：`ToolsTab` → `ReadmeTab`，使用 `react-markdown` + `remark-gfm` 渲染 SKILL.md

**验证**：TypeScript `tsc --noEmit` 0 错误；Python 模型/模板语法检查通过。

## 修复 sidecar 启动崩溃（NameError）

**根因**：`skill_rpc.py` 中 `SKILL_METHODS` dict 引用了 `_handle_skill_fix_manifest` 和 `_handle_skill_read_skill_md`，但这两个函数定义在 dict **之后**（Python 模块加载时 dict 字面量立即求值 → NameError）。

**修复**：将两个 handler 函数移到 `SKILL_METHODS` dict 之前。

**影响**：sidecar 无法启动 → 前端所有 Tauri invoke 都报 "管道正在被关闭 (os error 232)"。

## 补注册 Tauri Rust 桥接命令

**根因**：`skill_fix_manifest` 和 `skill_read_skill_md` 只在 Python sidecar 注册了 RPC handler，但从未在 Rust `#[tauri::command]` + `lib.rs` 注册。前端 `invoke("skill_read_skill_md", ...)` → Tauri 找不到对应 Rust 命令 → "Command skill_read_skill_md not found"。

- `skill.rs`：新增 `skill_fix_manifest` / `skill_read_skill_md` 两个 `#[tauri::command]`
- `lib.rs`：注册两个新命令
- 验证：`cargo check` 通过、`tsc --noEmit` 0 错误

## ErrorsTab 文案修订

- "manifest.json 必需字段" → "manifest.json 建议字段"
- 新增 `software_version: {min, max}` 字段说明

## 设置页"常规"Tab 实现

**需求**：界面设置页需要"常规"页面，让用户可修改 nexus-tool 的默认超时时间。

**排查发现**：
- Python `app_settings.py` 后端已完整实现（RPC: get/set/reset，字段: nexusToolDefaultTimeoutSec/nexusToolMaxConcurrent/nexusToolKillProcessTree/logLevel）
- Rust `commands/app_settings.rs` 命令已注册
- `nexus_tool_rpc.py` 的 `_resolve_timeout()` 已读 app.settings 作为超时来源（优先级：manifest > app.settings > fallback 120s）
- 前端缺失：IPC 方法 + UI Tab

**实现内容**：

1. `packages/apps/web/src/ipc/openclaw.ts` — 新增：
   - `AppSettings` / `AppSettingsResponse` 接口
   - `getAppSettings()` / `setAppSettings()` / `resetAppSettings()` 函数

2. `packages/apps/web/src/features/settings/settings.reducer.ts` — 修改：
   - `Tab` 类型加入 `"general"`
   - 默认 tab 改为 `"general"`

3. `packages/apps/web/src/components/settings/SettingsPage.tsx` — 修改：
   - 新增 `Sliders` / `RotateCcw` 图标导入
   - 新增 IPC 方法导入
   - TabsList 加入"常规"触发按钮（首位）
   - 新增 `GeneralTab` 组件（390 行），含 4 个设置项：
     - 默认工具超时（秒）1~86400
     - 最大并发工具数 1~64
     - 取消时递归杀子进程（toggle）
     - 保存 / 恢复默认 按钮
   - （递归杀子进程、日志等级两个设置项按用户要求移除，不开放给前端）

4. `packages/adapters/openclaw/wrapper/src/artifex_nexus/openclaw_wrapper/app_settings.py` — 修正：
   - `handle_app_settings_set` 返回中补充 `defaults` 字段（原来缺失）

## 技能页面 Skill 分页修复 + 分页顺序调整

**问题**：技能页面 Skill 分页无显示（API 返回空列表时无空状态 UI 提示）
**需求**：Skill 分页移至 Nexus-Tool 后面，默认显示 Tool 分页

**修复内容**：
1. `packages/apps/web/src/components/skills/SkillsPage.tsx` — 调整：
   - 默认 tab 从 `"skills"` → `"nexus_tools"`
   - Tab 顺序：Nexus-Tool 在前，Skill 在后
2. `packages/apps/web/src/components/skills/SkillList.tsx` — 修复：
   - 新增 `!loading && !error && skills.length === 0` 空状态 UI（Inbox 图标 + 提示文案）
   - 修复 `layerToSource` 返回类型为 `SkillSource`（解决 Record 索引类型错误）
3. `packages/apps/web/src/components/skills/NexusToolList.tsx` — 修复：
   - 新增同理空状态 UI

## 常规设置页文字大小调整

**问题**：GeneralTab 全部用 `text-[11px]`，与其他 Tab 的 `text-sm`(14px)/`text-xs`(12px) 层次不一致，视觉偏小。

**修改**（`packages/apps/web/src/components/settings/SettingsPage.tsx` GeneralTab）：
- 设置项标签：`text-[11px]` → `text-sm font-medium text-foreground`（14px）
- 描述文字：`text-[11px]` → `text-xs`（12px）
- 默认值/文件路径提示：`text-[10px]` → `text-[11px]`
- 卡片内部间距：`space-y-1.5` → `space-y-2`

## 常规设置页默认值提示与输入框间距修复

**问题**：`默认: 120s` / `默认: 3` 提示文字作为 `<input>` 的兄弟节点垂直排列，视觉上紧贴输入框。

**修改**（`packages/apps/web/src/components/settings/SettingsPage.tsx` GeneralTab）：
- 工具超时组：`<input>` + `<span>默认: ...</span>` 包裹进 `<div className="flex items-center gap-2 mt-1">`
- 最大并发组：同上
- 效果：输入框与默认值提示水平排列，居中对齐，`gap-2`（8px）间距

## Skill 列表为空修复（根因：SkillHub 未扫描项目 source layers）

**问题**：Skill 页面显示"暂无 Skill"，实际项目 skills/ 下有 8 个 skill（3 official + 5 marketplace）。

**根因**：`_rpc_helpers.py` 的 `_get_skill_hub()` 创建 SkillHub 时只传 `skills_root`（安装目录 `~/.artifexnexus/.openclaw/workspace/skills/`），未传 `layer_sources`。SkillHub 仅扫描 `99_custom` 层（空目录），项目技能全不可见。

**参考**：ArtClawToolManager 的 `skill_service.py` 扫描两处：
- `~/.openclaw/workspace/skills/`（已安装）
- `{project_root}/skills/{layer}/{dcc}/SKILL.md`（源码）

**修复**（`packages/adapters/openclaw/wrapper/src/artifex_nexus/openclaw_wrapper/_rpc_helpers.py`）：
- 新增 `_find_project_root()` — 向上查 `pnpm-workspace.yaml`
- 新增 `_find_skill_layer_sources()` — 返回 `{"00_official": Path, "01_marketplace": Path}`
- 修改 `_get_skill_hub()` — SkillHub 初始化时传入 `layer_sources=_find_skill_layer_sources()`

## Sidecar 加载失败修复（skill.list crash）

**症状**：`skill.list` RPC 返回 `'NoneType' object has no attribute 'name'`，前端显示"sidecar 未就绪"

**根因链**：
1. `load_manifest_model()` 内部捕获 pydantic ValidationError 返回 None（不抛异常）
2. `_load_entry_from_manifest()` 的 `try/except` 未触发（无异常传播），直接访问 `manifest.name` → AttributeError
3. 触发条件：6 个 manifest.json 的 `software: "unreal"` 不在 enum 白名单（应为 `"unreal_engine"`）
4. 且上一轮会话正常是因为 `_find_skill_layer_sources()` 还未加入（项目 skills 目录未被扫描）

**修复**：
1. `packages/platform/skill/src/artifex_nexus/skill/hub/core.py` — `_load_entry_from_manifest()` 改为显式 `if manifest is None: return None`
2. 6 个 `skills/**/manifest.json` — `"software": "unreal"` → `"unreal_engine"`
3. `packages/adapters/openclaw/wrapper/.../sidecar.py:2015` — `pkg_dir.parents[5]` → `_find_project_root()`（修复 SDK 路径 `packages\packages\` 重复）

## Skill 卡片三态按钮逻辑（对标 ArtClaw Tool Manager）

**问题**：当前 skill 卡片只有"已安装/已禁用"两态，未安装的 skill 不显示"安装"按钮

**修改**：
1. `skill_rpc.py` — `_handle_skill_list` / `_handle_skill_search` 增加 `installed` 字段（检查 `02_user/<name>` 目录存在性）
2. `skill-api.ts` — `SkillItem` 接口增加 `installed: boolean`
3. `SkillList.tsx` — 三态按钮矩阵：

| 状态 | 标签 | 按钮 |
|------|------|------|
| `installed=false` | 未安装 (amber) | 安装 / 详情 / ★ |
| `installed=true, enabled=false` | 已禁用 (red) | 启用 / 卸载 / 详情 / 📌 / ★ |
| `installed=true, enabled=true` | 已安装 (green) | 禁用 / 卸载 / 详情 / 📌 / ★ |

**验证**：`pnpm --filter @artifex-nexus/web build` 通过

## Sidecar skill.list 再崩溃修复（SkillInstaller 参数变更）

**症状**：`skill.list` RPC 报 `TypeError: SkillInstaller.__init__() got an unexpected keyword argument 'config'`

**根因**：`_rpc_helpers.py` 的 `_get_skill_installer()` 调用 `SkillInstaller(hub=..., config=_get_skill_config())`，但 SkillInstaller 已按"用户偏好独立于安装器"原则重构，`config` 参数改为 `config_path: Optional[Path]`。`_get_skill_config()` 返回 SkillConfig 对象，类型不匹配。

**修复**（`packages/adapters/openclaw/wrapper/src/artifex_nexus/openclaw_wrapper/_rpc_helpers.py`）：
- `config=_get_skill_config()` → `config_path=_DEFAULT_CONFIG_PATH`

## Skill/Tool 卡片发布、同步、删除按钮补齐

**问题**：Skill 卡片缺少"同步/更新"和"发布"按钮；Tool 卡片缺少"发布"和"删除"按钮

**修改**：

1. `SkillList.tsx` — 新增导入 `skillSync`/`skillPublish`，在 actions 中新增：
   - **同步**按钮：`skill.installed && !layer.startsWith("02_")`（官方/市场源 → 同步最新版本）
   - **发布**按钮：`skill.installed && layer.startsWith("02_")`（用户层 → 发布到团队/官方）

2. `NexusToolList.tsx` — 新增导入 `nexusToolPublish`/`nexusToolDelete`，在 actions 中新增：
   - **发布**按钮：`tool.source === "user"`（用户工具 → 发布到市场/官方）
   - **删除**按钮：`tool.source === "user"`（用户工具 → 删除）

**最终按钮矩阵**：

| 卡片类型 | 条件 | 按钮 |
|---------|------|------|
| Skill 未安装 | installed=false | 详情 / 安装 / ★ |
| Skill 已安装+禁用 | installed=true, enabled=false | 详情 / 启用 / 卸载 / 📌 / 同步(非用户层) / 发布(用户层) / ★ |
| Skill 已安装+启用 | installed=true, enabled=true | 详情 / 禁用 / 卸载 / 📌 / 同步(非用户层) / 发布(用户层) / ★ |
| Tool 通用 | 所有 | Info / 运行 / 启用触发(有触发器) / 📌 / ★ |
| Tool 用户源 | source="user" | + 发布 / 删除 |

**验证**：`npx tsc --noEmit` 通过

## Tool 卡片去钉选 + 运行/启用触发样式交换

**修改**（`NexusToolList.tsx`）：
- 移除 📌 钉选按钮及相关导入 `PinOff/Pin/nexusToolPin/nexusToolUnpin`
- "运行"按钮从 `variant="outline"` 改为 `size="sm"`（实心突出）
- "启用触发"按钮从 `size="sm"` 改为 `variant="outline"`（次要）

## Skill 面板多选 + 一键安装

**修改**（`SkillList.tsx`）：
- 新增状态：`selectMode`, `selectedIds` (Set<string>), `batchInstalling`
- 工具栏新增 ☑ 按钮进入选择模式
- 选择模式下显示：`x/y 选中` 计数 · `全选` · `一键安装 (N)` · `取消`
- 卡片：选择模式下显示 checkbox，`selected` 态高亮边框
- 选择模式下未安装 skill 隐藏单个操作按钮（统一用批量安装）
- `selectableCount` = 过滤后未安装 skill 数量
- 全选/反选仅针对未安装 skill
- 一键安装调用 `skillBatch("install", ids)`，完成后刷新列表并退出选择模式

## Skill 安装/批量安装无效修复（根因：Installer 源路径错误）

**症状**：点击"安装"/"一键安装"按钮，RPC 返回 OK(0ms) 但 skill 未实际安装。

**根因**：`SkillInstaller.install()` 在 `self._root / source_layer / skill_name`（即 `~/.artifexnexus/.openclaw/workspace/skills/00_official/...`）下找源文件，但实际源文件在项目目录 `skills/official/...` 和 `skills/marketplace/...`。

**修复**：
1. `packages/platform/skill/src/artifex_nexus/skill/installer.py`：
   - `__init__` 新增 `layer_sources: Optional[Dict[str, Path]]` 参数
   - 移除 `_layer_dir()` / `_skill_dir()` 方法
   - 新增 `_source_skill_dir(source_layer, name)`：优先用 `layer_sources`，回退到 `_root`
   - 新增 `_target_skill_dir(target_layer, name)`：始终指向 `_root`
   - `install` / `uninstall` / `sync` / `publish` / `sync_all` 全部更新为使用新方法
2. `_rpc_helpers.py`：`_get_skill_installer()` 传入 `layer_sources=getattr(hub, "_layer_sources", None)`
3. `skill_rpc.py`：`_skill_dir("02_user", ...)` → `_target_skill_dir("02_user", ...)`（2 处）
4. `SkillList.tsx`：
   - `doAction` 检查 `result.ok === false` 时显示错误信息
   - 错误提示改为内联 banner（始终可见，不是仅空列表时显示）

**验证**：`npx tsc --noEmit` 通过

**验证**：`npx tsc --noEmit` 通过

## Skill 目录结构扁平化 + 安装流程修复

**用户调整**：源码目录去掉 DCC 软件名文件夹层，skills/ 下只区分 official/marketplace（用户类不在源码中）。

**问题**：manifest.name 用下划线（`comfyui_node_installer`），目录名用连字符（`comfyui-node-installer`），导致 `_source_skill_dir` naive 路径拼接失败。

**修复**：
1. `installer.py`：`_source_skill_dir` 增加 fallback 扫描 —— naive 路径不存在时，递归查找 manifest.json 匹配 name 字段。
2. `skill_rpc.py`：`_handle_skill_install` 和 `_handle_skill_sync` 不再硬编码 `source_layer="00_official"`，改为自动从 Hub entry 检测（支持 marketplace 层：`01_marketplace`）。

**验证**：模拟测试确认所有 name/dir 不匹配的 skill（official 16 个中 ~14 个有下划线/连字符差异，marketplace 5 个中 ~3 个）都能通过 fallback 扫描正确定位。

**目录映射（新结构）**：
- 源：`skills/official/` (→ `00_official`), `skills/marketplace/` (→ `01_marketplace`)
- 目标：`~/.artifexnexus/.openclaw/workspace/skills/{official,team,user}`

## Skill 目标目录扁平化（OpenClaw 要求）

**用户要求**：目标（安装）目录需要扁平化，所有 Skill 直接放在 `workspace/skills/` 下，不分 official/team/user 子目录。

**修改**：
1. `installer.py`：
   - `_target_skill_dir(target_layer, skill_name)` → 简化为 `self._root / skill_name`（忽略 target_layer）
   - `_source_skill_dir` else 分支：`self._root / source_layer` → `self._root`（扁平回退）
   - `publish()` → 改为 metadata-only（扁平化后 source==target，无需文件复制）
   - `sync_all()` → 迭代 `self._root.iterdir()` 替代 `self._root / target_layer`
   - 更新类文档 + DEFAULT_LAYERS 注释
2. `docs/specs/skill-system.md`：更新 §6 分层与冲突，新增源码目录 vs 安装目标的区分说明

**目录映射（最终）**：
```
源（项目，分层管理）:              目标（安装，扁平）:
skills/official/      00_official    workspace/skills/{skill-name}/
skills/marketplace/   01_marketplace  ↑ 全部直接在这，无子目录
```

**注意**：`LAYER_PRIORITY` 未包含 `01_marketplace`（marketplace skills 优先级=999），目前无冲突（命名无重叠），可后续添加。

## Git 提交

- `b6252bb` — refactor(skills): flatten skill source directory structure（171 文件，+18368/-87）
  - 将所有 Skill 目录扁平化 + 安装/同步/发布流程修复 + 前端三态按钮/批量安装等一并提交

## Nexus-Tool 触发器启用/禁用持久化（Phase A + B）

方案文档：`docs/nexus-tool-trigger-persistence.md`

### Phase A: Manifest 格式清理
- 3 个 bundled manifest 删除旧格式冗余字段 `trigger:{}` 和 `execution:{}`，统一为新扁平格式
- `trigger_dispatcher.py:_load_tools()` 改用 `t.get("triggerType")` / `t.get("dcc")` / `t.get("eventType")` 替代旧的 `t.get("trigger",{})` 嵌套
- `模型批量加前缀后缀/manifest.json` → triggers=[]（无改动）
- `SM命名检查/manifest.json` → 从旧格式完整迁移到新扁平格式
- `Blender对象命名规范检查/manifest.json` → 删除冗余旧字段
- **description 字段保留**，与旧格式清理无关

### Phase B: 补上工具总闸的 dispatcher 缺口
- `trigger_dispatcher._load_tools()` 中新增 `SkillConfig` 读取，获取 `get_disabled_nexus_tools()`
- 加载工具时检查 tool_id 是否在禁用列表，是则跳过注册
- 有 disabled tools 时打印 info 日志

### 不改动
- 维度3（DCC 总闸）：已在 Blender 插件面板实现，符合预期

## 紧急修复：Blender 侧 trigger_dispatcher 格式同步 + 旧实例兜底

**问题1**：Phase A 只改了 sidecar 侧 dispatcher，遗漏 Blender addon 内的 `trigger_dispatcher.py` → 保存钩子失效。

**问题2**：用户实例工具 `Example-Blender对象命名规范检查 (实例)-01` 在 Phase A 之前创建，manifest 含旧格式，且第二个触发器缺少新格式 `dcc` 字段 → 打开触发失效。

**修复**：
- Blender 侧 dispatcher `_load_tools()` 同步改为新扁平格式
- 两个 dispatcher 加新旧格式 fallback：`t.get("triggerType") or t.get("trigger",{}).get("type")` 模式
- 实例 manifest：清理旧字段 + 补 `"dcc":"blender"`
- 补漏 `official/universal/` 下 2 个 compliance-checker manifest

**教训**：manifest schema 变更必须全局搜索所有读取点（sidecar dispatcher + 各 DCC addon dispatcher + 用户实例目录）

## 全局 manifest 旧格式扫描

全项目 29 个 manifest.json 扫描结果：
- **5 个**含 triggers：全部已修复为扁平格式
- **24 个** triggers=[]（skills 的 manifest，无需处理）
- **0 个**旧格式残留 ✅

## 实例工具 file.open.post 触发失效修复（根因×2）

**症状**：`Example-Blender对象命名规范检查 (实例)-01` 设置了文件打开触发，但不生效。

**根因 1 — tool-sources.json 缺少用户工具目录**：
- `~/.artifexnexus/nexus-tools/user/` 从未被注册到 `tool-sources.json`
- Blender trigger_dispatcher 的 `_get_source_dirs()` 从 `tool-sources.json` 读取源目录，扫描不到用户实例工具
- Sidecar trigger_dispatcher 同样受影响（`get_all_manifest_paths()` 读同一配置文件）

**根因 2 — 实例工具目录下无 main.py**：
- 实例工具只有 `manifest.json`，无 `main.py`（脚本沿用父工具）
- 两个 trigger_dispatcher 的 `_execute_tool()` 都直接用 `tool_dir` import `main`，没有 `parentPath` fallback
- importlib.import_module("main") 在实例目录下必然失败

**修复（6 个文件）**：

| 文件 | 修改 |
|------|------|
| `tool-sources.json` | 手动添加 `~/.artifexnexus/nexus-tools`（`user` 类型） |
| `bootstrap.py:_register_default_tool_sources()` | 新增注册用户工具目录 |
| `sidecar.py main()` | 启动时自动确保用户工具目录已注册 |
| `tool_sources.py:register_source()` | 新增 `_normalize_path()` 剥离 `\\?\` 前缀防去重失效 |
| `trigger_dispatcher.py (sidecar)` | `_execute_tool()` 加 parentPath fallback |
| `trigger_dispatcher.py (Blender)` | `_execute_tool()` 加 parentPath fallback |

**parentPath fallback 逻辑**：
```python
entry_path = Path(tool_dir) / entry
if not entry_path.exists():
    parent_path = manifest.get("parentPath", "")
    if parent_path and Path(parent_path).is_dir():
        tool_dir = parent_path  # 使用父工具目录 import
```

**教训**：
- `tool-sources.json` 是三端的唯一数据源（sidecar dispatcher + Blender dispatcher + 共享配置），新增目录类型必须确保注册环节闭环
- 实例工具的脚本 fallback 是隐式约定，需在 dispatcher 层显式实现

## Blender 侧 trigger_dispatcher 补工具总闸禁用检查

**症状**：Nexus-Tool 卡片点「禁用触发」后卡片状态更新，但 Blender 里仍会触发。

**根因**：Sidecar dispatcher 在 `_load_tools()` 中读 `SkillConfig.get_disabled_nexus_tools()` 跳过禁用工具，但 Blender dispatcher 未做此检查（Blender 无法 import `artifex_nexus.core.skill_config`）。

**修复**（`trigger_dispatcher.py` Blender 侧）：
- 新增 `SKILLS_CONFIG_PATH` 常量 (`~/.artifexnexus/config/skills.json`)
- 新增 `_get_disabled_nexus_tools()` — 直接读 JSON 获取禁用列表（返回 `set`）
- `_load_tools()` 中：读取 `disabled_tools`，`tool_id in disabled_tools` 则 `continue` 跳过

**三维度启用/禁用矩阵（修复后）**：

| 维度 | 存储 | Sidecar | Blender |
|------|------|:---:|:---:|
| 1. per-trigger `enabled` | manifest.json | ✅ | ✅ |
| 2. tool-level 总闸 | skills.json | ✅ | ✅ (new) |
| 3. DCC-level 总闸 | Blender 插件面板 | N/A | ✅ |

## 触发器系统规范文档更新

更新了两份 spec 文档，方便后续 DCC 开发：
1. `docs/specs/dcc-extension-trigger-system.md` — 主规范（+170 行），新增：
   - 三维度启用/禁用矩阵（§1.2）
   - manifest v2 扁平格式 + 新旧 fallback（§4.1-4.2）
   - 实例工具 parentPath fallback（§4.6）
   - 工具总闸 skills.json 直读（§5.3）
   - 完整 _load_tools() 伪代码（§5.4）
   - 配置写入流程（§8）— 补 user 源 + sidecar 启动期确保
   - 扩展到其他 DCC 验收清单（§9.2）
2. `docs/nexus-tool-trigger-persistence.md` — 决策记录，新增：
   - 完整修复记录（6 项）
   - 教训汇总（5 条）

## Tool/Skill 卡片/列表视图持久化 + Skill List 实时性确认

**问题 1**：视图切换（卡片/列表）没有持久化记录，切 Tab 或刷新页面后重置回默认。

**修复**（3 个文件）：
- `SkillsPage.tsx` — `activeTab` 持久化到 localStorage (`artifex.skills.activeTab`)
- `SkillList.tsx` — `viewMode` 持久化到 localStorage (`artifex.skills.skillViewMode`)
- `NexusToolList.tsx` — `viewMode` 持久化到 localStorage (`artifex.skills.toolViewMode`)

使用 localStorage 安全读写模式（try/catch 兜底 SSR），与 ChatControlBar.tsx 已有模式一致。

**问题 2 确认**：Skill list 是实时扫描的，非固定列表。
- 后端 `_handle_skill_list` → `hub.scan_all_skills()` → `self._entries.clear()` → 遍历 `_layer_sources` 递归扫描 manifest.json
- 每次 RPC 调用都重新扫描文件系统，无缓存
- 前端 `SkillList.tsx` 在 `useEffect` 初始化加载 + 操作完成后 `loadSkills()` 刷新
- 如果文件系统在页面已打开后新增 skill，可点刷新按钮手动触发 `loadSkills()`

## Skill 发现机制重构：以 SKILL.md 为唯一源

**问题**：64 个 SKILL.md 只有 21 个 manifest.json。扫描依赖 `rglob("manifest.json")`，43 个 skill 被跳过。

**方案**：
- SKILL.md = 识别标志 + name/description 唯一源
- manifest.json = 补充属性（version/category/software/tools 等），可选
- 无 manifest.json → 从 SKILL.md `metadata.artclaw.*` 构建合成 manifest
- 校验失败不跳过，记录 `validation_error` 在前端卡片显示

**修改（7 个文件）**：

1. `manifest/models.py` — SkillManifest 放宽约束：
   - `_NAME_PATTERN`: 允许连字符 `[a-z0-9_-]`
   - `version` → 默认 "0.0.0"，缺省值跳过 semver 校验
   - `software` → 默认 "unknown"（可选字段）
   - `skill_tools` → 允许空列表
   - 移除 `manifest_version` 严格 "1.0" 校验
   - `software` 类型从 `Software` enum → `str`

2. `hub/core.py` — SkillHub 扫描重构：
   - `_scan_layer`: `rglob("SKILL.md")` 替代 `rglob("manifest.json")`
   - 新增 `_parse_skill_md_frontmatter()`: 解析 YAML frontmatter
   - 新增 `_build_skill_entry()`: 从 SKILL.md 构建 SkillEntry
     - 先解析 SKILL.md 取 name/description
     - manifest.json 存在→加载并覆盖 name/description 为 SKILL.md 的值
     - manifest.json 不存在→从 `metadata.artclaw.*` 构建合成 manifest
     - 始终返回 entry（失败也返回，记录 validation_error）
   - SkillEntry 新增 `validation_error` 和 `description` 属性

3. `_rpc_helpers.py` — `_entry_to_dict` 新增 `description` 和 `validation_error` 字段

4. `installer.py` — 兼容 SKILL.md：
   - 新增 `_is_skill_dir()`: 检查 SKILL.md 或 manifest.json 存在
   - `_source_skill_dir` fallback 扫描加入 SKILL.md frontmatter 匹配
   - `install()` / `sync_all()` / `publish()` 全部改用 `_is_skill_dir()`

5. `skill-api.ts` — `SkillEntry` 接口新增 `description` 和 `validation_error`

6. `SkillList.tsx` — 卡片显示：
   - `description`: 优先 SKILL.md 的 description
   - `titleBadge`: 有 validation_error 时显示"格式问题"标签
   - 错误信息显示在 description 区（⚠ 前缀）

**验证**：
- 64 个 SKILL.md → 64 个 SkillEntry（之前只有 21 个）✅
- 0 个 validation_error
- 有 manifest.json 的 skill 属性正确合并（version/category/software 保留）
- TypeScript 编译 0 错误 ✅
- Python import 无错误 ✅

**manifest.json name/description 保留不动**（用户说"看代码修改量"），list 加载时 SKILL.md 覆盖。

## 2026-05-19 16:38 — Skill 格式规范与右侧面板改造

### 需求
1. 缺少 manifest.json 的技能显示 ⚠ "格式问题" 标签
2. 卡片数据源严格分离：name/description 仅从 SKILL.md，其他字段仅从 manifest.json
3. Skill 详情从弹窗改为右侧面板（对齐 Tool 的实现）
4. 错误详情在右侧面板子分页显示

### 变更文件 (6)
- `hub/core.py` — `_build_skill_entry`: manifest.json 缺失时 validation_error="缺少 manifest.json"，合成 manifest 不再读 metadata.artclaw
- `_rpc_helpers.py` — `_entry_to_dict`: 扩展 has_manifest / author / tags / risk_level / dependencies / entry_point / license / skill_tools
- `skill-api.ts` — SkillEntry 新增对应 TS 字段，category 改为 string|null
- `SkillDetailPanel.tsx` (新) — 3 标签页右侧面板（基本信息/工具列表/格式问题）
- `SkillList.tsx` — 移除 Dialog，改用 PreviewContext→D5 面板；titleBadge 双重条件
- `RightPanel.tsx` — PreviewRenderer 新增 kind="skill-detail"

### 验证
- TS: 0 errors
- Python scan: 64 skills, 43 标记缺少 manifest.json, 21 OK
- 缺失 manifest 的技能: version=0.0.0, software=unknown, category=None (全部默认)

## 2026-05-19 17:11 — manifest.json 模板 + 一键修复

### 需求
1. 创建 manifest.json 模板文件作为唯一格式源，其他脚本引用此源
2. 格式问题面板增加「一键修复」按钮，从 SKILL.md 自动生成 manifest.json

### 变更文件 (7)
- `template.json` (新) — manifest.json 标准模板，所有字段含默认值
- `manifest_fixer.py` (新) — 核心修复逻辑：
  - `generate_manifest_from_skill_dir()` — 解析 SKILL.md → 生成 manifest dict
  - `fix_manifest()` — 一站式：解析 + 写入磁盘
  - 软件名规范化映射 (ue→unreal_engine, sd→substance_designer 等)
  - 分类名规范化映射
- `manifest/__init__.py` — 导出 fix_manifest, generate_manifest_from_skill_dir
- `skill_rpc.py` — 注册 `skill.fix_manifest` RPC
- `_rpc_helpers.py` — 新增 `_skill_fix_manifest()` 辅助函数
- `skill-api.ts` — 新增 `skillFixManifest()` API
- `SkillDetailPanel.tsx` — ErrorsTab 增加「一键修复」按钮 + 结果反馈

### 验证
- Python: 成功从 artclaw-unity-context SKILL.md 生成完整 manifest.json
  - name: artclaw_unity_context (连字符→下划线)
  - 从 metadata.artclaw 提取: display_name/author/software/category/version/tags
  - 从 SKILL.md top-level 提取: license
  - 默认值: risk_level=low, entry_point=__init__.py
- TypeScript: 0 errors

## SkillDetailPanel 增加安装路径显示

**需求**：已安装的 Skill 除了显示源路径，还要显示安装路径。

**修改（3 个文件）**：
1. `skill_rpc.py` — `_handle_skill_detail` 中通过 `installer._target_skill_dir()` 计算安装路径，若目录存在则返回 `install_path`
2. `skill-api.ts` — `SkillDetail` 接口新增 `install_path?: string | null`
3. `SkillDetailPanel.tsx` — InfoTab 新增 `installPath` prop，在源路径下方显示安装路径（仅当安装路径存在且与源路径不同时）；两个路径均可点击调用 `shell_open_path` 在文件管理器中打开

**验证**：`tsc --noEmit` 0 错误；`cargo check` 通过；Python skill_rpc 16 methods OK

## Gitignore 审查与修复

**审查发现 2 个问题**：
1. `openspec/changes/*` 忽略了 2 个子目录的设计文档（m3-design-language + openclaw-reinstall-confirm），共 10 个文件
2. `docs/specs/_spikes/*.json` 忽略了 2 个 OpenClaw schema 参考文件

**修复**：
- 移除 `openspec/changes/*` 和 `!openspec/changes/.gitkeep` 规则
- 移除 `docs/specs/_spikes/*.json` 和 `!docs/specs/_spikes/.gitkeep` 规则
- 同步更新注释
- commit `c56a613` → push to GitHub，+53,900 行

## Skill 卡片/列表标题点击打开详情

**需求**：点击 Skill 卡片或列表标题/图标也能打开详情面板。

**修改**：`SkillList.tsx` — `ItemCard` 增加 `onTitleClick={() => handleDetail(skill.name)}`
- ItemCard 已有 `onTitleClick` prop，卡片视图应用于图标+标题，列表视图同理
- NexusToolList 早已有此配置，无需改动

## 软件枚举扩展：添加 substance_painter / substance_designer / unity

**唯一源**：`packages/platform/contracts/data/categories.json` 添加 3 个值 + 中文显示名

**同步更新（6 个文件）**：
- `categories.json` — software 数组 + display.software
- `manifest.schema.json` — software.enum
- `categories.py` — `_builtin_fallback()`
- `skillsMock.ts` — `DCC` 类型 + `DCC_LABELS`
- `AtMentionDialog.tsx` — `DCC_ICONS`（P/D/N）
- `SkillList.tsx` / `NexusToolList.tsx` — `DCCIcon` colors/icons

**验证**：Python `Software` enum 现含 10 个值（含新增 3 个）

## Skill 详情页可编辑 + 同步检测 + 保存 Manifest

**需求**：详情页基本信息支持编辑；仅已安装 Skill 可编辑；保存到安装目录不动源码；提示发布

**后端新增（2 个 RPC + 2 个辅助函数）**：
- `_rpc_helpers.py`：
  - `_entry_to_dict` 补 `software_version` 字段
  - 新增 `_skill_check_sync(skill_name)` — 调用 `compare_skill_dirs()` 返回同步状态
  - 新增 `_skill_update_manifest(skill_name, fields)` — 读/改/写安装目录 manifest.json，pydantic 校验
- `skill_rpc.py`：
  - `_handle_skill_detail` 附加 `sync_state`
  - 新增 `skill.check_sync` + `skill.update_manifest` RPC handler（共 18 methods）

**前端层**：
- `skill-api.ts`：`SkillEntry` 补 `software_version`，`SkillDetail` 补 `sync_state`，新增 `skillCheckSync` / `skillUpdateManifest` API
- Rust `skill.rs` + `lib.rs`：注册 `skill_check_sync` / `skill_update_manifest` 命令

**SkillDetailPanel.tsx InfoTab 可编辑改造**：
- 标题栏增加"可编辑"标签（仅已安装）
- 同步状态横幅：needs_update（青色）/ needs_publish（琥珀色）
- 软件：下拉列表（从 DCC_LABELS 动态获取），版本约束 min~max 文本输入
- 版本：文本输入 + semver 格式校验（红色边框 + 提示不合法时）
- 其他字段：文本输入（分类/作者/风险等级下拉/许可证/入口/标签/依赖）
- 保存按钮 → `skillUpdateManifest` → 成功后显示"发布到源目录"按钮
- 只读时正常显示原有格式

**验证（全链路）**：Python 18 methods OK；`tsc --noEmit` 0 错误；`cargo check` 通过

## 格式规范改名 + 移除 risk_level + 编辑体验优化

### 1. 格式规范改名
- `SkillDetailPanel.tsx` 3 处 "ArtClaw 格式规范" → "artifex nexus skill 格式规范"
- "ArtClaw manifest.schema.json" → "artifex nexus manifest.schema.json"

### 2. 移除 risk_level 字段（7 个文件）
- `template.json`：移除 `"risk_level": "low"`
- `manifest_fixer.py`：移除兜底模板 + 提取代码 + docstring 中的 risk_level
- `_rpc_helpers.py`：allowed_fields 移除 "risk_level" + 序列化移除 risk_level 行
- `models.py`：删除 `risk_level` 字段、`RiskLevel`/`ALL_RISK_LEVELS` 导入，更新 docstring
- `manifest/__init__.py`：移除 `RiskLevel` 导入和 `__all__` 条目
- `skill-api.ts`：`SkillEntry` 移除 `risk_level` 字段
- `SkillDetailPanel.tsx`：移除 riskLevel state + 编辑 UI + RISK_LABELS 常量

### 3. 格式模板统一源确认
- `template.json` 是 Python 侧的"唯一格式源"（manifest_fixer.py:5 注释），一键修复使用它
- 前端 ErrorsTab 有硬编码副本（现已同步更新）
- 后端 "universal" vs 前端 "general" 命名不一致问题仍然存在，SkillsList 筛选已加互认映射

### 4. Tab 切换修复
- 切换卡片时始终先 `setActiveTab("info")`，有 validation_error 再切 errors

### 5. 软件下拉增加通用选项
- `softwareOptions` 不再过滤 `k !== "general"`
- `SkillList.tsx` 筛选修复：`general`（前端）↔ `universal`（后端）互认

### 6. 编辑变更检测
- `hasChanges` useMemo 比较所有表单字段与 entry 原始值
- 保存按钮 `disabled={saving || !versionValid || !hasChanges}`，无修改时标题提示"无修改"

### 7. 双列字段布局
- software + software_version 保持全宽
- 其他字段 grid-cols-2：版本|分类、作者|许可证、入口文件|标签
- 依赖 + 层级保持全宽

**验证**：`tsc --noEmit` 0 错误；`cargo check` 通过；Python SkillManifest 向后兼容（extra="allow" 保留 risk_level）

## 大规模重构：DCCEntry + category→tags + 合规检查器更新

**范围**：7 项改动，~42 个文件

### Step 1-3: 后端数据层 + 业务层 + 模板
- `nexus_tool/models.py` — 新增 `DCCEntry` dataclass (dcc/minVersion/maxVersion)；`target_dccs: List[DCCEntry]`
- `nexus_tool/scanner.py` — 解析新旧两种 targetDCCs 格式：`string[]` (旧) → `[{dcc,...}]` (新)
- `nexus_tool/registry.py` — L197 non_general 提取改用 `_dcc_names`
- `categories.py` — 删除 `ALL_CATEGORIES`/`CATEGORY_DISPLAY`/`CATEGORY_PATTERN`；`_builtin_fallback()` 移除 category
- `manifest/models.py` — 删除 `category` 字段、`_validate_category`、`is_custom_category`、`_category_pattern`
- `manifest/template.json` — 删除 `"category": "utils"`
- `manifest/manifest_fixer.py` — `_CATEGORY_NORMALIZE` 改为局部函数；category 值合并入 tags 首部
- `manifest.schema.json` — 删除 `category` 字段定义
- `hub/core.py` — 删除 `category` property；`list_entries(category=)` → `list_entries(tags=[])`（OR 匹配）
- `hub/instance.py` — 删除 `SkillToolInfo.category` 字段 + `SkillInstance.category` property
- `registry.py` — `list_by_software(category=)` → `tags`；`search()` 中 `entry.category` → `entry.manifest.tags`
- `skill_rpc.py` — `category` 参数 → `tags`（向后兼容旧参数）；删除 tool info 中 category 序列化
- `decorator/core.py` — 删除 `@skill_tool(category=...)` 参数
- `__init__.py` — 删除 `ALL_CATEGORIES`/`CATEGORY_DISPLAY`/`CATEGORY_PATTERN` 导出；新增 `DCCEntry` 导出
- `nexus_tool/__init__.py` — 新增 `DCCEntry` 导出
- `_rpc_helpers.py` — `_nt_data_to_dict()`: `target_dccs` 序列化为 `[{dcc, minVersion, maxVersion}]`；删除 category 引用；`allowed_fields` 移除 category
- `nexus_tool_rpc.py` — L505/592/623 三处 DCC 名提取：`e.dcc.lower() for e in` 替代直接 `.lower()` for 字符串

### Step 6: 8 个 bundled manifest 迁移
Python 脚本批量：`targetDCCs: ["xxx"]` → `[{"dcc": "xxx"}]`；删除 `source` 字段

### Step 7: 合规检查器更新
- `tool-compliance-checker/main.py`：
  - `valid_dccs` 更新为 10 个 ALL_SOFTWARE 值（替代旧的 ue5/maya2024 等不一致枚举）
  - targetDCCs 迭代兼容 DCCEntry 格式 + 旧 string 格式
  - 删除 Rule 12 (source 字段检查)
  - Rule 13 (id 检查) 改为 UUID 格式优先
- `skill-compliance-checker/main.py`：
  - `_VALID_SOFTWARE` 更新为 10 个值
  - `_check_enum_fields()`: targetDCCs 兼容 DCCEntry 格式 + 新增 tags 格式检查

### Step 4-5: 前端类型 + 组件层
- `nexus-tool-api.ts` — 新增 `DCCEntry` 接口；所有 `target_dccs: string[]` → `DCCEntry[]`
- `skill-api.ts` — 删除 `SkillEntry.category`/`SkillToolItem.category`；`SkillListFilters.category` → `tags?: string[]`
- `skillsMock.ts` — 新增 `DCCEntry` 导出；备注 DCC_LABELS 为 fallback
- `SkillDetailPanel.tsx` — 删除 category 状态/编辑 UI；分类 → 标签输入框；ErrorsTab 检查改为 tags
- `SkillList.tsx` — `skill.category` → `skill.tags?.join(", ")`
- `NexusToolList.tsx` — DCC 筛选/图标/显示 适配 DCCEntry 格式
- `RightPanel.tsx` — DCC 分组兼容 DCCEntry
- `RunPanel.tsx` — DCC 名称提取适配
- `ToolDetailPanel.tsx` — 状态类型 `string[]` → `DCCEntry[]`；toggleDCC 改用 `some/e.dcc`
- `FiltersTab.tsx` — prop 类型 → `DCCEntry[]`；内部提取 dcc 名
- `TriggerRuleEditor.tsx` — prop 类型放宽为 `(string | {dcc: string})[]`；迭代提取 dcc 名

**验证**：`tsc --noEmit` 0 错误 ✅

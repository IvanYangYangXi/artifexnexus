# 2026-05-25

### Maya/Max 插件版本修正 + 端口冲突处理 + 优化 (15:06)

**版本修正 v5.0.0 → v2023**:
- 重命名目录: `packages/dcc/{maya,max}/src/artifex_nexus/v5.0.0/` → `v2023/`
- Maya: `plugin_info.version = (2023,)`，去掉冗余 `maya_min`；`DCC_VERSION = "2023"`
- Max: `plugin_info.version = (2023,)`，`max_min = (2023,)`；`DCC_VERSION = "2023"`
- Max trigger_dispatcher `python.execute` 字符串中 `v5_0_0` → `v2023`
- dcc_installer.py `_get_addon_src_dir()` 使用 `startswith("v")` 自动发现，无需修改

**端口冲突处理（参考 UE 方案）**:
- 共享 SDK `MCPServer.__init__` 新增 `max_port_probe: int = 10` 参数
- Maya/Max `create_server()` 传入 `max_port_probe=0`（固定端口，不探测）
- Maya/Max `start_server()` 端口预检查：被占用时 `_is_port_available` → UI 警告 → return False
- Maya 弹 `cmds.confirmDialog(icon="warning")`，Max 输出 `pymxs.runtime.print(msg, warning=True)` + `print()`

**优化 — 提取共享 trigger_dispatcher_base**:
- 新建 `packages/dcc/shared/artifex_nexus_sdk/trigger_dispatcher_base.py` → `TriggerDispatcher` 类
- 提取重复逻辑：`_is_duplicate`, `_load_tool_sources`, `_find_matching_tools`, `_execute_trigger_entry`, `handle_post_save`, `handle_post_open`
- Maya/Max `trigger_dispatcher.py` 重写为薄层（仅保留事件钩子注册/注销）
- 行数减少：Maya 193→78 行，Max 164→90 行

**QA 验证**:
- ✅ 11 个 Python 文件语法检查全部通过
- ✅ 无残留 `v5.0.0`/`v5_0_0` 引用
- ✅ 端口 18081/18082 一致，max_port_probe=0
- ✅ Spec 文档同步更新（版本号 + locale 复制方式）

### UE 触发器系统实现（STORY-0056）

**问题**：UE 工具运行正常但触发器无法生效，前端提示"触发器需要 DCC 插件支持"。

**根因分析**：
1. 前端 `dccStatus` 名称 `"Unreal"` 与 manifest 的 `dcc: "unreal_engine"` 不匹配 → `hasConnectedDCC = false`
2. UE 缺少 manifest 驱动的触发器调度引擎（Blender 有 `BlenderTriggerDispatcher`，UE 只有旧版 `triggers.json` 格式）
3. UE 已有 C++ delegate 注册 + `dcc_event_intercept.py` 功能代码，但未接入 manifest 扫描

**修改文件（8个）**：

| 文件 | 改动 |
|------|------|
| `packages/apps/web/.../AppShell.tsx` | dccStatus name: `"Unreal"` → `"unreal_engine"`, `"Blender"` → `"blender"` |
| `packages/apps/web/.../Topbar.tsx` | 添加 `DCC_DISPLAY` 映射表 |
| `Content/Python/trigger_dispatcher.py` | **新建** — UE 进程内触发器引擎 |
| `Content/Python/dcc_event_intercept.py` | **重写** — 从 triggers.json 改为委托 manifest 驱动 dispatcher |
| `Content/Python/ue_mcp_server.py` | 新增 `broadcast_trigger_event()` |
| `Content/Python/init_unreal.py` | MCP 启动后注入状态上报回调 |
| `.../sidecar/trigger_dispatcher.py` | 解除 Blender-only 限制，支持 `unreal_engine` |
| `docs/tasks/backlog/STORY-0056-ue-trigger-system.md` | 更新进展日志 |

**架构变更**：
- UE 触发器从旧 `triggers.json` 格式迁到 manifest 驱动（对齐 Blender 新架构）
- C++ 接口契约（`check_pre_save` 等 6 个函数签名）完全保持不变
- 全局开关联动 C++ `bTriggersEnabled`（每次事件触发时同步）

**已同步到测试工程**：`D:\MyProject_D\artifexnexus_packages\ue57_artifex_nexus\`

### UE 触发器调试日志增强 + 不触发根因定位（2026-05-25 11:06）

**问题**：触发器错误提示消失但实际不触发。用户用 "Example-UE-SM-命名检查" 测试。

**根因**：manifest 中唯一的 save 触发器 `on-static-mesh-pre-save`（`eventType: "asset.save.pre"`）设置了 `"enabled": false`。保存资产时 C++ 调用 `check_pre_save` → dispatcher 跳过禁用触发器 → NO MATCH。

另外两个启用的触发器 `f25ace5e`（`asset.place.post`）和 `4e8fadcd`（`asset.import.post`）只在放置/导入资产时触发，不在保存时触发。

**调试日志增强**：
- `trigger_dispatcher.py`：添加 `_ue_log()` / `_ue_warn()` 辅助函数，在 UE Output Log 可见
- 关键路径：启用/禁用状态、manifest 扫描结果、触发器匹配/跳过、工具加载汇总
- 被禁用的 trigger 会输出 `SKIP disabled trigger [id] eventType (enabled=false)` 到 Output Log
- `dcc_event_intercept.py`：在每个 C++ 调用入口添加 `_ue_log()` 输出事件类型和路径

**修改文件**：
- `packages/dcc/unreal/.../trigger_dispatcher.py`：+_ue_log/_ue_warn + 6 处调试日志
- `packages/dcc/unreal/.../dcc_event_intercept.py`：+_ue_log + 4 处入口日志
- 已同步到测试工程

**建议测试步骤**：
1. 重启 UE → 打开 Output Log → 搜索 `[ArtifexNexus|Trigger]`
2. 检查是否输出 "已加载: X 个工具, Y 条触发器规则"
3. 保存资产 → 应该看到 "NO MATCH for event=asset.save.pre"（因为 trigger #1 enabled=false）
4. 在 artifex 前端将 `on-static-mesh-pre-save` 触发器启用 → 重新保存 → 应看到 MATCHED 并执行

### 面板颜色 + 触发器实时生效 + Log 刷屏修复（2026-05-25 11:24）

**确认**：拖入场景触发正常（之前是 UE 面板的 triggers 关闭了）。

**三个修复**：

1. **C++ 面板按钮颜色**：
   - `ArtifexNexusPanel.h`：新增 `GetTriggerButtonColor()` 声明
   - `ArtifexNexusPanel.cpp`：按钮绑定 `ButtonColorAndOpacity`，文字改为 "Triggers: Enabled"（绿）/ "Triggers: Disabled"（红）
   - 按钮颜色：Enabled → `FLinearColor(0.1f, 0.6f, 0.1f)`（绿）；Disabled → `FLinearColor(0.8f, 0.1f, 0.1f)`（红）
   - **需要重新编译 UE 插件**

2. **触发器实时生效（Dispatcher 自动重载）**：
   - `trigger_dispatcher.py`：新增 `_should_auto_reload()` 方法
   - 每次事件触发时检查 `tool-sources.json` + `skills.json` 的 mtime
   - 任一文件 mtime 变化 → 自动调用 `reload_tools()`
   - 冷却期 5 秒防止频繁重载
   - `_load_tools()` 结束后记录各配置文件 mtime
   - **无需额外操作，下次 DCC 事件触发时自动生效**

3. **LogUdpMessaging 刷屏**：
   - `DefaultEngine.ini`：添加 `[Core.Log] LogUdpMessaging=Error` 抑制警告级别日志
   - 原因：局域网内另一台 UE 实例（`10.107.8.32`）发送的陈旧 UDP 消息段
   - 重启 UE 生效

**修改文件**：
- `Source/ArtifexNexus/Public/ArtifexNexusPanel.h`：+1 declaration
- `Source/ArtifexNexus/Private/ArtifexNexusPanel.cpp`：+2 binding + implementation + text change
- `Content/Python/trigger_dispatcher.py`：_should_auto_reload + mtime tracking
- `Config/DefaultEngine.ini`（测试工程）：LogUdpMessaging 抑制

### C++ 编译修复 + MCP 触发重载（2026-05-25 11:37）

**C++ 编译错误修复**：
- `ButtonColorAndOpacity` 需要 `FSlateColor` 而非 `FLinearColor`
- `ArtifexNexusPanel.h`：`FLinearColor` → `FSlateColor`
- `ArtifexNexusPanel.cpp`：返回值包裹 `FSlateColor(FLinearColor(...))`
- 已同步到主项目和测试工程

**MCP 触发 `reload_triggers`**：
- `universal_proxy.py` `_build_context()` 注入 `reload_triggers` 可调用函数
- 调用 `UETriggerDispatcher.get_instance().reload_tools()`
- 前端/sidecar 通过 `run_python` 执行 `reload_triggers()` 即时重载
- 自动重载机制（mtime 检测）作为兜底

**UDP 说明**：
- UE 默认启用 "UDP Messaging" 插件，用于多用户编辑和设备发现
- `10.107.8.32` 是局域网内另一台 UE 实例，编辑器自动广播发现消息
- 无害但刷屏，已通过 `DefaultEngine.ini` 抑制日志
- 如需完全禁用：UE 菜单 → Edit → Plugins → 搜索 "UDP Messaging" → 禁用

### 逐条触发器实时生效修复（2026-05-25 11:45）

**问题**：卡片级启用/禁用触发能实时生效，但详细面板里逐条触发器启用/禁用无法实时生效。

**根因**：`trigger_dispatcher.py` 的 `_should_auto_reload()` 只监控 `tool-sources.json` + `skills.json`，不监控工具的 `manifest.json`。
- 卡片级启用/禁用 → 写 `skills.json` → mtime 变化被检测 → 实时生效 ✅
- 逐条触发器启用/禁用 → 写工具的 `manifest.json` → mtime 变化未被检测 → 不生效 ❌

**修复**（3 处改动）：
1. `__init__`：新增 `_manifest_mtimes: Dict[str, float]` 字典
2. `_load_tools()`：加载每个 manifest 后记录 `{path: mtime}`
3. `_should_auto_reload()`：新增 manifest mtime 变更检测循环

**修改文件**：
- `packages/dcc/unreal/ArtifexNexusForUnreal_57/Content/Python/trigger_dispatcher.py`
- 已同步到测试工程 `D:\MyProject_D\artifexnexus_packages\ue57_artifex_nexus\`

**工作原理**：用户在前端修改逐条触发器的启用/禁用 → sidecar 写 `manifest.json` → 下次 UE 事件触发时 `_should_auto_reload()` 检测到 manifest mtime 变化 → 自动调用 `reload_tools()` 重载 → 新状态即时生效。冷却期 5 秒防抖。

### UE 触发器默认启用 + Blender 实时生效 + GitHub 提交（2026-05-25 11:56）

**UE 触发器默认启用**：
- `ArtifexNexusSubsystem.h`：`bTriggersEnabled` 默认值 `false` → `true`
- 打开 UE 时触发器面板默认显示绿色 "Triggers: Enabled"
- 已同步到测试工程

**Blender 实时生效检查**：
- **发现**：Blender 的 `trigger_dispatcher.py` 完全没有自动重载机制
- 只有 `reload_tools()` 方法但从未被 `on_trigger_event()` 调用
- 无 `_should_auto_reload()`、无 mtime 追踪

**Blender 修复**（对齐 UE，4 处改动）：
1. `__init__`：新增 `_config_mtimes` / `_manifest_mtimes` / `_last_load_time` / `_reload_cooldown`
2. `_load_tools()`：记录每个 manifest.json 的 mtime + config mtime
3. 新增 `_should_auto_reload()` 方法（同 UE 逻辑）
4. `on_trigger_event()`：`elif self._should_auto_reload(): self.reload_tools()`

**GitHub 提交**：`35c1d4f` — 14 files, +1095/-556，已推送到 main 分支

### Blender addon 全量同步（2026-05-25 12:08）

**问题**：用户报告 Blender 删除工具条目后触发器仍在执行。

**根因**：Blender 实际加载的 addon 位于 `%APPDATA%/Blender Foundation/Blender/5.1/scripts/addons/artifex_nexus/`，与主项目源码是**两个独立副本**。主项目中刚添加的 `_should_auto_reload()` 自动重载机制完全没有部署到 Blender 运行目录：
- `trigger_dispatcher.py`：缺少全部 auto-reload 代码（`_should_auto_reload`、mtime 追踪等）
- `__init__.py`：1104 行差异，整个 addon 严重过期

**修复**：
- 全量同步 `packages/dcc/blender/.../blender_addon/` → `%APPDATA%/.../addons/artifex_nexus/`
- 需在 Blender 中禁用→重新启用 addon（或重启 Blender）加载新代码

**部署提醒**：今后对 Blender addon 的每次修改都必须同步到 `%APPDATA%` 路径。UE 插件同理需同步到测试工程。

### Maya & 3ds Max MCP 集成 — 全栈开发完成（2026-05-25 13:29）

**范围**：STORY-0059 ~ 0066（8 个 STORY），Spec + STORY 卡片 + EPIC/Board + 14 个代码文件。

**产出清单**：
- Spec: `docs/specs/maya-max-mcp-integration.md`
- STORY ×8: `docs/tasks/backlog/STORY-0059~0066-*.md`
- EPIC-0007: 标题改为 "UE 5.7 + Maya + 3ds Max"
- Board: backlog 列 +8 STORY

**代码变更（21 files, +2100/-250）**：

| 层 | 新建文件 | 修改文件 |
|----|---------|---------|
| 共享 SDK | `base_adapter.py`, `mcp_server.py` | - |
| Maya 插件 | `pyproject.toml`, `__init__.py`, `maya_adapter.py`, `mcp_server.py`, `trigger_dispatcher.py` | - |
| Max 插件 | `pyproject.toml`, `__init__.py`, `max_adapter.py`, `mcp_server.py`, `trigger_dispatcher.py`, `startup.py`, `artifex_startup.ms` | - |
| Gateway | - | `index.ts` (+maya/max servers+tools), `openclaw.plugin.json` (+contracts) |
| Sidecar | - | `dcc_installer.py` (+Maya/Max install/uninstall + locale sync + _get_addon_src_dir 参数化), `bootstrap.py` (+servers) |
| 前端 | - | `openclaw.ts` (+IPC types), `dccRegistry.ts` (+maya/max entries) |
| Blender | - | `blender_adapter.py` (import SDK), `mcp_server.py` (重写), `__init__.py` (+SDK path) |
| Blender | 删除 `base_adapter.py` | - |

**端口**：18080 UE / 18081 Maya / 18082 Max / 18083 Blender / 19789 Gateway（无冲突）
**验证**：Python syntax ×10 files ✅ | 端口 ✅ | `pnpm tauri build` ⏳ | Maya/Max 实际环境 ⏳

### junction → 纯复制 + QA 审核（2026-05-25 14:40）

**junction → copy**：Maya/Max locale 同步从 `_link_or_copy_dir`（未定义！）改为 `shutil.copytree` 纯复制。`_cleanup_maya_locales` / `_cleanup_max_locales` 改为 `shutil.rmtree`。

**QA 发现并修复**：
- **P0-1**: `_link_or_copy_dir` 未定义 → 已修复为 `shutil.copytree`
- **P0-2**: `_get_addon_src_dir("3ds_max")` 路径错误（key 是 `3ds_max` 但目录是 `max`）→ 已添加 `_DCC_PKG_DIR` 映射
- **P0-3**: `mcp_bridge.py` Maya/Max 端口用旧值（18084/18085 → 18081/18082）→ 已修复
- **P1-1**: Max `__init__.py` `_print_status` 中 "Maya→Max fix" copy-paste 残留 → 已修复
- **P1-2**: Max `_ensure_consumer` import 路径 `v5_0_0` 与目录 `v5.0.0` 不匹配 → 已改为 `from max_adapter import`
- **P1-3**: Max `adapter.on_startup()` 从未被调用 → 已在 `register()` 中添加
- **P1-4**: Max `_global_adapter` 未在 addScript 前设置 → 已在 `_get_adapter()` 中同步设置

### 构建修复 (15:33)

**MCPBridgeStatus 类型错误**: `AppShell.tsx` 和 `SystemPage.tsx` 中 `ipc.MCPBridgeStatus` 无法从 `getIpc()` 返回值推断 → 显式 `import type { MCPBridgeStatus }`。

**chat-service.ts degraded 类型错误**: `ws.state === "degraded"` 不在 `WsConnectionState` 中（degraded 时连接仍是 `connected`）→ 改为 `ws.state === "connected"`。

### Maya/Max 安装向导自动检测 (15:40)

**问题**：安装向导中 Maya/3ds Max 缺少自动检测功能。前端 IPC 已定义但缺后端实现。

**实现**:
1. `dcc_installer.py`: 新增 `_parse_plugin_info()` / `get_dcc_plugin_info()` / `check_dcc_version_compatibility()` 三个通用函数，解析 Maya/Max 的 `plugin_info` dict（不同于 Blender 的 `bl_info`）
2. `sidecar.py`: 新增 6 个 RPC handler（Maya/Max detect/install/uninstall）+ METHODS map 注册
3. `openclaw.rs`: 新增 6 个 Tauri command（maya_detect/install/uninstall + max_detect/install/uninstall）
4. `lib.rs`: 注册 6 个新命令

**检测逻辑**:
- Maya: 扫描 `~/Documents/maya/` 版本目录 → `is_dcc_addon_installed("maya")` → `check_dcc_version_compatibility("maya")`
- Max: 扫描 `%LOCALAPPDATA%/Autodesk/3dsMax/` 版本目录 → `is_dcc_addon_installed("3ds_max")` → `check_dcc_version_compatibility("3ds_max")`
- Max 版本目录支持 `2024` 和 `2024 - 64bit` 两种格式

**构建**: 全量通过（Next.js + Rust + NSIS）

### DCC 插件扩展总文档重写 (15:54)

重写 `docs/specs/dcc-plugin-management.md` 作为 DCC 扩展主文档：

**结构**: 13 节 — 架构总览 → 模块清单（16 项必改模块）→ SDK API 参考（BaseDCCAdapter / MCPServer / TriggerDispatcher）→ 版本号规范 → 安装向导（检测链路 / 扫描路径 / 安装目标 / Locale 同步 / UE 特殊处理）→ RPC 接口 → Rust 命令 → Gateway 插件 → 前端 IPC → 已完成 DCC 引用 → 端口总表 → 函数索引

**更新过时内容**:
- Maya=18081, Max=18082（非 18084/18085）
- 版本格式 v2023（非 v5.0.0）
- 安装方式统一 copy（非 junction/symlink）
- 新增 TriggerDispatcher SDK 说明
- 新 DCC 接入清单从 10 项扩展为 16 项

**同步更新**: `docs/sdk/README.md` — 修正过时代码路径（`packages/sdk/` → 实际路径），新增 MCPServer / TriggerDispatcher SDK 条目

**已知过时**: `docs/sdk/dcc-installer.md` — 仍含 junction/symlink 引用和旧版本号格式，待后续更新

### QA 验证：文档完整性 + Maya/Max 接入完整性 (16:01)

**输出**: `.workbuddy/qa/dcc-maya-max-2026-05-25.md`

**文档审查结果**:
- 13 节结构完整，5 个 P2 问题（字段名不准确 D1-D3、Rust 路径 D4、函数索引遗漏 D5）

**Maya 接入验证** (16 模块逐项):
- ✅ 16/16 主线完整
- 4 个 P2：install 未调用 set_dcc_port、plugin_info 缺 maya_min、dcc-preinput 缺 provider、无 userSetup.py 自动生成

**Max 接入验证** (16 模块逐项):
- ✅ 16/16 主线完整
- 4 个 P2：同 Maya 2 项 + 硬编码版本路径 + plugin_info 字段命名不统一
- Max 额外亮点：MaxScript 启动入口 + 启动脚本自动部署

**结论**: 无 P0/P1 阻断，Maya/Max 接入已达到可用状态

### 安装向导 Maya/Max 检测修复 (16:30)

**根因**: `get_dcc_plugin_info()` / `get_addon_info()` 路径双重拼接。`_get_addon_src_dir()` 已返回含 `{addon_dir_name}` 的完整路径（如 `.../v2023/maya_addon/`），但两个 info 函数又拼了一次 `addon_dir/`，导致 `.../maya_addon/maya_addon/__init__.py` 路径不存在，回退到 hardcoded fallback `(0,0,0)`。

**修复 (4处)**:
1. `get_addon_info()`: `src_dir / "blender_addon" / "__init__.py"` → `src_dir / "__init__.py"`
2. `get_dcc_plugin_info()`: 删除冗余 `addon_dir` 拼接
3. `_get_addon_src_dir()`: 移除不一致 fallback `return entry`（不含 addon dir），统一返回含 addon 子目录的路径
4. `InstallerWizard.tsx`: `installPath` 从 Blender 硬编码改为 DCC 感知的 `getDCCInstallBasePath()`

**验证**: `get_dcc_plugin_info('maya')` → `{version:(2023,), dcc_min:(2023,)}` ✅ | TypeScript ✅ | Tauri full build ✅

### 移除 hardcoded fallback (16:51)

4 个函数的 hardcoded 返回值改为 raise，强制从真实 __init__.py 读取：
- `get_addon_info()`: 删除 2 处 hardcoded return → FileNotFoundError / 透传
- `_parse_bl_info()`: 删除 hardcoded return → ValueError
- `get_dcc_plugin_info()`: 删除 2 处 hardcoded return → FileNotFoundError / 透传
- `_parse_plugin_info()`: 删除 hardcoded return → ValueError

### InstallerWizard 空 catch 修复 (16:55)

`handleGlobalDetect` 中 Maya/Max 检测的 catch 块为空，所有错误静默吞掉。
→ 改为 `addLog(item.id, "error", e.message)` 输出到日志面板。
Tauri full build 通过。

### Maya/Max 检测重写：注册表 + Program Files 扫描 (17:06)

**根因**：`find_maya_versions`/`find_max_versions` 只扫描用户偏好目录（`~/Documents/maya/`、`%LOCALAPPDATA%/Autodesk/3dsMax/`），不扫描真实安装路径。

**修复**：重写为三层检测策略（同 UE `_find_ue_python` 模式）：
1. Windows 注册表: `HKLM\SOFTWARE\Autodesk\Maya` / `HKLM\SOFTWARE\Autodesk\3dsMax`
2. Program Files 目录: 扫描 `Maya{year}` / `3ds Max {year}` 目录 + 验证 `bin/maya.exe` / `3dsmax.exe` 存在
3. 用户偏好目录回退

**前端调试**：`handleGlobalDetect` 循环中加入 `console.warn` 追踪每个 item id + dccActions + state；入口处加 `console.warn` 打印 items 列表。
Tauri full build 通过。

### SystemPage.tsx 补全 Maya/Max 检测+安装+卸载 (17:30)

**根因**：用户看到的"系统→安装向导"是 `packages/apps/web/src/components/system/SystemPage.tsx` 的 `InstallerTab`，与 `apps/desktop/src/routes/InstallerWizard.tsx` 是**两套独立代码**。之前所有改动都在后者，前者完全没有 Maya/Max。

**修复**（SystemPage.tsx）：
- `handleGlobalDetect`: +Maya +Max 检测（IPC detectMayaVersions/detectMaxVersions + children 构建）
- `handleInstall`: +Maya +Max 分支（引导用户展开后点击子项安装）
- `handleChildInstall`: +Maya +Max 分支（installMayaAddon/installMaxAddon + 重装先卸载）
- `handleDeleteChild`: +Maya +Max 分支（uninstallMayaAddon/uninstallMaxAddon）

Tauri full build 通过。同时清理了 InstallerWizard.tsx 中的 console.warn 调试日志。""

### 删除无用 InstallerWizard 代码 + 修复 3ds Max 注册表检测 (17:55)

**删除**: 16 个无用文件
- `apps/desktop/src/routes/InstallerWizard.tsx` + `.module.css`
- `apps/desktop/src/features/installer/` 目录（14 个文件）
- `apps/desktop/src/App.tsx` 中移除 InstallerWizard 导入和 /installer 路由
- 原因：SystemPage.tsx 实际渲染安装向导，InstallerWizard.tsx 从未被访问

**3ds Max 检测修复** — `dcc_installer.py::find_max_versions()`:
- **根因**：注册表 `HKLM\SOFTWARE\Autodesk\3dsMax` 子键是 Autodesk 内部版本号 `"25.0"` 而非年份 `"2023"`。`.isdigit()` 对带点号字符串返回 False。
- **版本映射公式**: `year = 1998 + major_version`（如 25 -> 2023）
- **修复**：
  1. 新增 `_max_registry_key_to_year()` 函数，支持 "25.0" -> 2023 和 "2023" -> 2023 双格式
  2. 策略 1：通过 Installdir/Location 值验证是否实际安装（过滤空键如废弃的 23.0/24.0）
  3. 新增策略 2：Windows 卸载列表扫描（Uninstall registry），正则匹配 "3ds Max 20XX"
  4. 原策略 2/3 顺延为 3/4
  5. Program Files 扫描增加 ProgramW6432 环境变量支持

**构建**: Next.js ✓ Compiled 10.1s ✓ Linting ✓ Exporting | Rust ✓ 1m14s | NSIS ✓

**追加修复** (18:12): handleGlobalDetect 中第 339 行 ID 漏改 `"max"` → `"3ds_max"`。导致检测正常返回 3 个 Max 版本但 setItems 匹配不上 fixture，UI 不显示。构建 ✓ 1m03s

**优化 + 文档 + 提交** (18:20):
1. **版本号示例优化**: 手动添加/编辑子项时各 DCC 显示对应格式 placeholder（Blender 4.3 / Maya 2023 / Max 2023 / UE 5.7）
2. **安装路径对照 artclaw**: Maya `~/Documents/maya/{ver}/scripts/` ✅ | Max `%LOCALAPPDATA%/Autodesk/3dsMax/{ver}/ENU/scripts/` ✅（artclaw 额外支持 `{ver} - 64bit` 格式，后续跟进）
3. **新建文档**: `docs/development/dcc-detection-guide.md` — 完整检测指南（注册表格式、版本映射、ID 陷阱、artclaw 对照）
4. **Git**: commit b55ba68 → push main（24 files, +578/-2986）

**综合修复** (18:31): 
1. **部署校验误报**: uninstall_dcc_addon 改先清 manifest 再删目录; validate_all_deployments 静默清理过期条目
2. **版本兼容**: 新增 get_available_plugin_versions + find_best_plugin_for_dcc; Maya/Max 安装前检查兼容 + 不兼容弹窗显示可用版本; 新 IPC openclaw_dcc_plugin_versions
3. **Max 64bit + locale**: _get_max_real_dirs 已有 `{ver} - 64bit` 支持; _sync_max_locales/_sync_maya_locales 覆盖 ENU/CHS/JPN
4. **Blender placeholder**: `4.3` → `5.1`
5. **Git**: commit 7f9c337 → push main（6 files, +244/-19）

**版本兼容检查修复 + 插件版本标签页** (18:50):
1. **_checkDCCPluginCompatibility 修复**: 永远 return true 的 bug → 返回 showConfirm 结果; 字符串版本比较 → 数值比较; dcc_max=None 从"无上限" → 严格匹配
2. **补齐 Blender/UE**: handleChildInstall 四个 DCC 全部加兼容检查
3. **Python 同步**: check_dcc_version_compatibility 改为严格匹配 + 合并 plugin_compat.json 覆盖; _parse_plugin_info 支持 Blender bl_info
4. **新标签页"插件版本"**: Package 图标; 展示所有 DCC 插件版本列表; 编辑兼容范围（持久化到 ~/.artifexnexus/config/plugin_compat.json）; 重置为内置默认值
5. **新 IPC 全链路**: get_all_plugins_with_compat / update_plugin_compatibility / reset_plugin_compatibility (Python → sidecar → Rust → TS)
6. **Git**: commit 123aac4 → push main（6 files, +540/-53）

**插件版本标签页添加 UE 支持** (19:26):
1. **UE 目录结构**: `packages/dcc/unreal/ArtifexNexusForUnreal_57/`（`_57` = UE 5.7，可并存多版本）
2. **元数据解析**: 新增 `_parse_ue_plugin_descriptor()` 解析 `.uplugin` JSON 的 `EngineVersion` 字段
3. **版本提取**: `_reverse_ue_version_suffix("57")` → `"5.7"`
4. **全链路**: get_available_plugin_versions/get_dcc_plugin_info 添加 UE 分支; sidecar 白名单; 前端 DCC_COLORS 添加 violet
5. **Git**: commit 3c8e111 → push main（3 files, +90/-9）

### 修复 Maya/Max IPC 缺失 + ID 不一致 (17:37)

**问题**: SystemPage.tsx 调用 `ipc.detectMayaVersions()` / `ipc.detectMaxVersions()` 报 `is not a function`。

**根因**: web 版 `packages/apps/web/src/ipc/openclaw.ts` 缺少 Maya/Max 的 TypeScript 封装函数，只有 Blender 的函数。Rust 后端已注册 `openclaw_dcc_maya_detect` 等命令，desktop 版 IPC 也定义了，但 web 包漏掉。

**额外发现** — SystemPage.tsx 中 3ds Max ID 不一致：
- Fixture 中 ID 是 `"3ds_max"`，但 `handleInstall`/`handleDeleteChild`/`handleChildInstall` 中用 `"max"` 做 ID 比较 → 死代码

**修复 (2 files)**:
1. `packages/apps/web/src/ipc/openclaw.ts`: +DCCDetectResult/DCCInstallResult/DCCUninstallResult 类型 + 6 个 IPC 函数（detectMayaVersions/installMayaAddon/uninstallMayaAddon/detectMaxVersions/installMaxAddon/uninstallMaxAddon）
2. `packages/apps/web/src/components/system/SystemPage.tsx`: 3 处 `"max"` → `"3ds_max"`

**构建**: Next.js ✓ Compiled (no errors) ✓ Linting ✓ Exporting | Rust `release` ✓ 1m07s | NSIS ✓ 安装包已生成

**修复取消安装卡"安装中" + Max 2023 版本比较误判** (19:33):

**Bug 1 — 取消安装后状态不回滚**:
- `handleChildInstall` 在兼容检查前设置 `state: "installing"`，用户取消后 return 不重置
- 修复：4 个 DCC 分支（UE/Maya/Max/Blender）取消时用 `prevState: ItemState` 变量重置状态

**Bug 2 — Max 2023 无法安装**:
- `check_dcc_version_compatibility` 中 `dv_parts` pad 到 3 位 `(2023,0,0)` 但 `dcc_min` 维持 `(2023,)`
- Python tuple 长度不同永远 != → 报"插件 v2023 仅兼容 2023"
- 修复：`dcc_min` 和 `dcc_max` 也 pad 到 3 位再比较

**修改文件**: SystemPage.tsx (4 个分支) + dcc_installer.py (check_dcc_version_compatibility) + staging 同步
**构建**: Next.js ✓ Linting ✓ Exporting | Rust ✓ 1m13s | NSIS ✓

**UE版本号截断 + Max安装目标修正 + Locale同步日志** (19:49):

**UE版本号只取前两位**:
- `_parse_ue_plugin_descriptor`: `parts` 从 pad 到 3 位改为只保留前 2 位 `(5, 7)`
- 插件版本页显示 "5.7" 而非 "5.7.0"

**Max 安装目标修正**:
- **根因**: `get_dcc_addon_target_dir("3ds_max", "2023")` 用简单模板 `{base}/{version}/ENU/scripts/` → 目标 `.../3dsMax/2023/ENU/`。但实际 Max 2023 装在 `.../3dsMax/2023 - 64bit/` 目录
- **修复**: `get_dcc_addon_target_dir` 对 3ds_max 先调 `_get_max_real_dirs`，优先选含 "64bit" 的实际目录名
- **artclaw 对照**: artclaw 也只装了 `2023/ENU/`，同样没处理 -64bit 变体
- **Maya**: 无此问题，`scripts/` = ENU，`zh_CN/scripts/` = CHS，locale 同步正常

**前端日志展示 locale 同步**:
- Maya: `✅ 安装成功 → {path} （已同步 zh_CN）`
- Max: `✅ 安装成功 → {path} （已同步 CHS，启动脚本已部署）`

**修改文件**: dcc_installer.py (UE 版本截断 + Max 目标修正) + SystemPage.tsx (locale 同步日志) + staging 同步
**构建**: Next.js ✓ Linting ✓ Exporting | Rust ✓ 1m13s | NSIS ✓
**构建**: Next.js ✓ Linting ✓ Exporting | Rust ✓ 1m13s | NSIS ✓

**两套代码讨论**:
- SystemPage.tsx（`packages/apps/web/`）— 实际渲染，"完全复刻"自 InstallerWizard
- InstallerWizard.tsx（`apps/desktop/src/routes/`）— 架构更优（useReducer+dccRegistry）但未使用
- 建议：低优先级删除 InstallerWizard.tsx 及相关 `features/installer/` 文件，但当前不阻塞功能

---
name: nexus-tool-creator
description: >
  Nexus-Tool Creator — AI 引导创建自定义 DCC 检查/修复工具。
  支持三种创建方式：包装 Skill、编写脚本、组合工具。
  Use when AI needs to: (1) create a new nexus-tool from scratch,
  (2) wrap an existing Skill as a parameterized quick-access tool,
  (3) compose multiple tools into a pipeline.
  All tools stored in ~/.artifexnexus/nexus-tools/user/{name}/.
  NOT for: running or executing tools (use nexus-tool-executor),
  installing skills (use nexus-skill-manage).
metadata:
  artifex_nexus:
    version: 1.1.0
    author: Artifex Nexus
    software: all
    tags: ["tool-creator", "nexus-tool", "development"]
---

# Artifex Nexus 工具创建器

AI 引导创建自定义 Nexus-Tool 的专项 Skill。用户通过对话界面完成工具创建流程。

## 触发条件

- 用户在工具管理面板中点击「创建工具」
- 用户发送 `/create nexus_tool` 命令
- 用户明确要求创建新的 DCC 检查/修复工具

## 创建流程状态机

```
start → select_method → collect_info → generate → preview → confirm → save → complete
```

1. **start**: 展示三种创建方式供选择
2. **select_method**: 用户选择：包装 Skill / 编写脚本 / 组合工具
3. **collect_info**: 收集必要信息（名称、参数、筛选条件等）
4. **generate**: 生成 manifest.json 和脚本文件
5. **preview**: 展示预览
6. **confirm**: 用户确认
7. **save**: 调用 `NexusToolInstaller.create_nexus_tool()` 写入 manifest.json；对 `script` 类型，AI 需额外将生成的脚本写入 `main.py`（`create_nexus_tool` 只写 manifest，不写脚本）
8. **complete**: 创建完成，提示发布或测试

## 三种创建方式

### 1. 包装 Skill (skill_wrapper)

将现有 Skill 包装为固定参数的工具。

```json
{
  "id": "<auto-generated-uuid>",
  "name": "快速 UV 重排（默认参数）",
  "implementation": {
    "type": "skill_wrapper",
    "skill": "dcc-node-graph-workflow",
    "fixedParams": { "target": "uv", "method": "island-average" }
  },
  "inputs": [
    { "id": "threshold", "name": "阈值", "type": "number", "default": 0.8 }
  ]
}
```

### 2. 编写脚本 (script)

使用 DCC 原生 Python API 或 `artifex_nexus_sdk` 创建自定义工具。

**生成的脚本模板**：

```python
"""工具名称 — 一句话描述。"""
# ── SDK 头（tool-creator 自动注入）──
import os, json
import artifex_nexus_sdk as sdk

def _load_manifest() -> dict:
    manifest_path = os.path.join(os.path.dirname(__file__), "manifest.json")
    with open(manifest_path, "r", encoding="utf-8") as f:
        return json.load(f)
# ── SDK 头结束 ──


def main_function(**kwargs):
    """入口函数。kwargs 由 Tool Manager 传入。"""
    manifest = _load_manifest()

    # ── 1. 参数解析 ──
    parsed = sdk.params.parse_params(manifest.get("inputs", []), kwargs)

    # ── 2. 对象获取 + 筛选 ──
    type_cfg = manifest.get("defaultFilters", {}).get("typeFilter", {})
    types = type_cfg.get("types", [])
    source = type_cfg.get("source", "selection")

    if source == "selection":
        explicit = parsed.get("target_paths", "")
        if explicit:
            objects = [{"path": p.strip(), "type": "", "name": p.strip().rsplit("/", 1)[-1]}
                       for p in explicit.split(",") if p.strip()]
        else:
            objects = sdk.context.get_selected_assets()
        if types:
            objects = sdk.filters.filter_by_type(objects, types)
        if not objects:
            return sdk.result.fail("NO_INPUT", "未指定目标，且当前无选中对象。")

    # ── 3. 业务逻辑 ──
    # import bpy     # Blender
    # import unreal  # UE
    # ...

    # ── 4. 结果上报 ──
    return sdk.result.success(data={}, message="完成")
```

> **脚本配置分层原则**（见下方 manifest schema → 配置分层决策）：所有路径范围、类型列表等规则配置统一写入 manifest，脚本只负责读取。禁止将它们定义为模块级常量。

### 3. 组合工具 (composite)

将多个现有工具串联成管线。暂不支持自动编排，AI 在对话中引导用户按序执行。

---

## manifest.json Schema

```json
{
  "id": "<uuid-v4-guid>",
  "name": "工具显示名称",
  "description": "工具描述",
  "version": "1.0.0",
  "author": "创建者名称",
  "createdAt": "2026-05-20 21:00:00",
  "updatedAt": "2026-05-20 21:00:00",
  "software": [{"dcc": "blender"}],
  "tags": ["modeling", "uv"],
  "source": "user",
  "parentPath": null,

  "implementation": {
    "type": "script|skill_wrapper|composite",
    "entry": "main.py",
    "function": "main_function",
    "skill": null,
    "agentHint": "AI 执行提示词"
  },

  "inputs": [],
  "outputs": [],
  "triggers": [],
  "presets": [],
  "defaultFilters": {
    "path": [],
    "typeFilter": {"source": "selection", "types": []}
  },
  "dependencies": []
}
```

### 配置分层决策

| 层 | manifest 字段 | 适合放什么 | 用户能否在 UI 改 |
|----|--------------|-----------|------------------|
| **用户参数** | `inputs[]` | 每次运行可能不同的值 | ✅ 运行时填写 |
| **规则配置** | `defaultFilters` | 工具生效范围 | ✅ 工具设置页 |
| **固定元数据** | 顶层其他字段 | 名称、版本、触发规则 | ❌ 仅开发者 |
| **❌ 禁止** | 脚本模块级常量 | 任何配置都不应写死在 `main.py` | ✗ 改了代码才生效 |

### software 推断规则

`software` 表示工具适用的 DCC 范围，格式为 `[{dcc, minVersion?, maxVersion?}]`。

| 场景 | software 值 |
|------|------------|
| 不涉及 DCC API 的通用工具 | `[]`（空数组） |
| 使用 artifex_nexus_sdk 跨 DCC API | `[]` |
| 仅特定 DCC | `[{"dcc": "blender"}]` |
| 多 DCC | `[{"dcc": "blender"}, {"dcc": "maya"}]` |

**有效 DCC 标识**（来自 `contracts/data/categories.json` 唯一数据源）：`general`, `unreal_engine`, `blender`, `maya`, `3ds_max`, `houdini`, `substance_designer`, `substance_painter`, `comfyui`, `unity`

### id 字段（UUID v4 GUID）

`id` 由 `NexusToolInstaller.create_nexus_tool()` 自动生成为 UUID v4，**用户不手动设置**。
GUID 确保改名/移动目录后信息不漂移。

### 触发规则范式

`triggers` 数组定义自动触发规则。支持三种类型：

#### event 类型

```json
{
  "id": "on-file-save",
  "name": "保存文件时检查",
  "enabled": true,
  "triggerType": "event",
  "dcc": "blender",
  "eventType": "file.save.pre",
  "executionMode": "notify"
}
```

**常用事件**：

| DCC | 事件名 | 说明 |
|-----|--------|------|
| blender | `file.save.pre` | 保存前拦截 |
| blender | `file.save.post` | 保存后通知 |
| blender | `render.post` | 渲染完成后 |
| unreal_engine | `asset.save.pre` | 资产保存前 |
| unreal_engine | `asset.save.post` | 资产保存后 |
| maya | `file.save.pre` | 文件保存前 |

- event trigger 的 `dcc` 必须在 `software` 范围内
- `software=[]` 的通用工具不能用 event trigger

#### schedule 类型

```json
{
  "triggerType": "schedule",
  "scheduleMode": "interval",
  "intervalMs": 1800000
}
```

#### watch 类型

```json
{
  "triggerType": "watch",
  "watchEvents": ["created", "modified"],
  "debounceMs": 3000,
  "useDefaultFilters": true,
  "filters": {}
}
```

路径统一走 `defaultFilters.path` 或 triggers 的 `filters.path`，使用 `$` 变量：
- `$skills_installed` → `~/.artifexnexus/.openclaw/workspace/skills`
- `$nexus_tools_dir` / `$tools_dir` → `~/.artifexnexus/nexus-tools`
- `$project_root` → 项目根目录
- `$home` → 用户主目录

### inputs 参数类型

| type | 说明 | 额外字段 |
|------|------|----------|
| `string` | 文本输入 | `multiline`, `placeholder` |
| `number` | 数值输入 | `min`, `max`, `step` |
| `boolean` | 勾选框 | — |
| `select` | 下拉选择 | `options`（字符串数组，必填） |
| `file` | 文件路径 | — |

---

## 验证清单

保存前验证：

- [ ] manifest.json 包含所有必需字段（id、name、implementation.type）
- [ ] 配置分层合规：路径范围 → `defaultFilters.path`，类型范围 → `defaultFilters.typeFilter`，业务常量 → `inputs[]`，⛔ 无模块级硬编码常量
- [ ] software 已按推断规则正确设置
- [ ] 脚本优先使用 `artifex_nexus_sdk`（params / filters / result）
- [ ] 脚本 DCC 专有操作使用原生 API（UE: `unreal`，Maya: `maya.cmds`，Blender: `bpy`）
- [ ] ⛔ 脚本不包含 `import subprocess` 或 `os.system`
- [ ] 入口函数使用 `**kwargs` 签名
- [ ] 入口函数返回 dict 或 sdk.result
- [ ] 触发规则合规：
  - [ ] event trigger 的 `dcc` 在 `software` 范围内
  - [ ] `software=[]` 的通用工具不使用 event trigger
  - [ ] watch trigger 路径走 `filters.path`（不走独立 `paths` 字段）
- [ ] **filters.path pattern 不使用花括号**（`*.{py,md}` 需拆为多条）

## DCC 原生 API 速查

| DCC | 导入 |
|-----|------|
| Blender | `import bpy` |
| Maya | `import maya.cmds as cmds` |
| UE | `import unreal` |
| Houdini | `import hou` |

> 优先使用 `artifex_nexus_sdk` 覆盖的通用操作（选中对象、类型筛选、参数解析）。
> DCC 专有操作使用上述原生 API。

## 文件结构

```
~/.artifexnexus/nexus-tools/user/{name}/
├── manifest.json    # 工具定义
└── main.py          # 脚本（仅 script 类型）
```

## 创建后自动合规检查

工具保存后**必须**运行 `tool-compliance-checker` 验证：

```python
import sys
from pathlib import Path

tool_dir = Path.home() / ".artifexnexus" / "nexus-tools" / "user" / "{name}"
checker_dir = Path("D:/MyProject_D/artifexnexus/tools/official/tool-compliance-checker")
sys.path.insert(0, str(checker_dir))
from main import check_compliance

result = check_compliance(tools_dir=str(tool_dir), source_only=False)

if not result.get("success"):
    issues = result.get("issues", [])
    print(f"⚠️ 合规检查发现 {result.get('issues_found', len(issues))} 个问题：")
    print(result["report"])
    # → AI 根据 issues 逐一修复 → 重新运行 check_compliance() → 直到 success=True
else:
    print("✅ 合规检查通过")
```

检查项：`import artifex_nexus_sdk` 是否存在、入口函数调用参数解析、返回值格式、DCC 工具是否有 `defaultFilters.typeFilter`、software 枚举、trigger 范式等。

{
  "id": "mcp-bridge",
  "name": "MCP Bridge",
  "description": "Bridge external MCP servers (WebSocket) into OpenClaw agent tools. Connects to Blender MCP Server for AI-driven 3D operations.",
  "version": "5.0.0",
  "main": "src/index.ts",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "servers": {
        "type": "object",
        "description": "MCP server definitions keyed by server name",
        "additionalProperties": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "enum": ["websocket"],
              "description": "Transport type (only websocket supported)"
            },
            "url": {
              "type": "string",
              "description": "WebSocket URL (for type=websocket)"
            },
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Whether this server is enabled"
            }
          },
          "required": ["type", "url"]
        }
      }
    }
  },
  "uiHints": {
    "servers": {
      "label": "MCP Servers",
      "placeholder": "Add MCP server configurations"
    }
  }
}

"""
OpenClaw Wrapper — Python sidecar for Artifex Nexus desktop shell.

Provides JSON-RPC 2.0 over stdio server (sidecar.py) and shared business logic
modules: bootstrap, doctor, ports, runtime.
提供 JSON-RPC 2.0 over stdio 服务端及共享业务逻辑模块。
"""

